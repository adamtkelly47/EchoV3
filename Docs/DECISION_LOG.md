Version: 1.0
Status: DRAFT
Owner: Echo Project
Last Updated: July 2026

# Decision Log

## Purpose

A chronological, accessible index of material product and engineering decisions, so design reasoning does not survive only inside chat history (CONSTITUTION.md: Documentation Philosophy). Entries here that rise to architectural significance get a full ADR in `Docs/decisions/`; smaller or purely clarifying decisions are recorded here only.

---

## 2026-07-20 — Phase 0 initiated; PROMPT.md digested

`PROMPT.md` was populated with the full Echo v3 build specification (32 sections, 29 phases). CONSTITUTION.md (approved) and DOMAIN_OWNERSHIP.md (draft) already existed and were substantially aligned with it. `Docs/REQUEST_LIFESTYLE.md` existed as an empty, misnamed file.

## 2026-07-20 — Contradictions identified in PROMPT.md (Section 32 item 11)

Per the Immediate Assignment's own requirement to identify contradictions and unresolved risks before proceeding:

1. `REQUEST_LIFESTYLE.md` vs. the `REQUEST_LIFECYCLE.md` referenced by CONSTITUTION.md and DOMAIN_OWNERSHIP.md — typo. **Resolved**: created `REQUEST_LIFECYCLE.md`, removed the empty misnamed file.
2. CONSTITUTION.md's "Related Documents" list and PROMPT.md Section 27's documentation list disagree on which docs should exist. **Resolved for Phase 0**: used PROMPT.md Section 29's actual Phase 0 deliverable list as scope (constitution, architecture map, ownership, dependency rules, coding standards, approval model, provenance model, security principles, model routing, testing strategy, initial ADRs) plus README/DECISION_LOG/CONTRIBUTING for immediate usability. Deferred `INTEGRATIONS.md` and `OPERATIONS.md` — see entry below.
3. **Domain catalog mismatch** — PROMPT.md Section 7's repo tree includes `actions`, `integrations`, `documents` domains not present in DOMAIN_OWNERSHIP.md's 13-domain catalog, which instead has `Capabilities`, `Knowledge`, `System`. **Resolved**: see `Docs/decisions/ADR_0005_domain_catalog_reconciliation.md`. User confirmed DOMAIN_OWNERSHIP.md as authoritative on 2026-07-20.
4. `integrations` as a domain conflicts with Provider Independence (providers are implementation details, not domains). Folded into the ADR_0005 resolution.
5. `actions` vs. `Approvals` — Constitution's Execution Ownership already assigns execution to Approvals; `actions` folds into `domains/approvals/` as an internal module, not a sibling domain. Folded into ADR_0005.
6. Redis named as a replaceable event transport (Section 3.7) while Constitution says event infrastructure shouldn't be scaffolded before it's needed and default delivery is synchronous in-process. Not a contradiction — noted as a sequencing risk in DOMAIN_EVENTS.md (Redis serves queue/cache/lock roles from Phase 1 regardless of when/if it becomes an event transport).

## 2026-07-20 — Domain catalog reconciliation decided

User selected "Treat DOMAIN_OWNERSHIP.md as authoritative" when asked how to reconcile the Section 7 repo tree against the domain catalog. Recorded as ADR_0005. `ARCHITECTURE.md`'s repository tree reflects this decision and supersedes PROMPT.md Section 7 for this repository.

## 2026-07-20 — Event catalog naming inconsistencies found

While consolidating DOMAIN_OWNERSHIP.md's per-domain Published/Subscribed event lists into `DOMAIN_EVENTS.md`, four subscriber references were found that don't exactly match any publisher's event name (`ProjectDeadlineChanged` vs. `DeadlineChanged`; `PortfolioUpdated` with no exact publisher match; `CalendarEventCompleted` with no publisher match; `CapabilityExecutionRequested` with no publisher match). Flagged in `DOMAIN_EVENTS.md`'s "Known Inconsistencies" section rather than silently corrected, since event names are owned by their publishing domain. **Resolved 2026-07-20** — see ADR_0007 below.

## 2026-07-20 — Event catalog naming mismatches resolved (ADR_0007)

Fixed all four mismatches found above, directly in `DOMAIN_OWNERSHIP.md`: renamed Projects' `DeadlineChanged` to `ProjectDeadlineChanged` (matching its own naming convention and Calendar's existing subscriber expectation); repointed Conversation's subscription from the nonexistent `PortfolioUpdated` to `PortfolioSnapshotCreated`; repointed Projects' subscription from the nonexistent `CalendarEventCompleted` to `CalendarEventUpdated` (Calendar has no completion concept for events); removed Approvals' subscription to `CapabilityExecutionRequested` entirely, since that name is command-shaped and directly conflicts with the Constitution's own Event Principles ("not intentions, not commands") — approval evaluation is a synchronous Application-layer pipeline stage (REQUEST_LIFECYCLE.md's Approval Checker), not an event subscriber. Recorded as ADR_0007 since the Approvals fix touches approval architecture/orchestration, both ADR-triggering areas per CONSTITUTION.md. `DOMAIN_EVENTS.md` updated to match; its "Known Inconsistencies" section replaced with "Resolved Inconsistencies".

## 2026-07-20 — INTEGRATIONS.md and OPERATIONS.md deferred

PROMPT.md Section 27 lists these among required documentation, but at Phase 0 no integrations or deployed operations exist to document. Creating them now would describe future phases before they're reached, which the Constitution's No Future Scaffolding Rule treats as a defect when applied to code and this project extends the same discipline to documentation. They will be created when Phase 10 (first real OAuth integration) and Phase 1 (first deployable containers) respectively reach the point of having real content to document.

## 2026-07-20 — A Neon key was reported "inside the repository" with no actual key found

User stated a Neon key was already committed to the repo before Phase 1 began. A full search (tracked, untracked, `.git`, `.claude`, `.agents`) found no real key — one `npg_` grep hit was a false positive (the grep pattern itself, stored in `.claude/settings.local.json`'s permission allowlist). User confirmed no real key existed yet; none was ever found in source control. No rotation was necessary. Recorded here because "I searched and found nothing" is a claim that needs a paper trail, per the Constitution's Negative Verification principle.

## 2026-07-20 — Phase 1 (Docker development foundation) completed

Resolved a container-list vs. persistence-model tension in PROMPT.md: Phase 1's literal container list includes "PostgreSQL," but ADR_0002 already established Neon as the sole system of record, and the user created a real Neon account for this project. Decided (ADR_0006) that local dev connects directly to a Neon branch via `DATABASE_URL` — no local Postgres container.

Neon connection was established via the Neon MCP OAuth flow initially, which required two attempts (the first callback URL's flow state had expired by the time it was submitted — likely a delay between authorizing and pasting the URL back). The user ultimately entered the connection string into `.env` manually rather than through MCP.

Two real defects were found and fixed during verification (not assumed away):

1. The Ollama container's healthcheck used `wget`, which does not exist in the `ollama/ollama` image (`wget: not found`). Fixed by using `ollama list` (the CLI itself talks to the local server over HTTP).
2. The frontend container's healthcheck (`curl http://localhost:3000/`) failed with "Failed to connect" despite Next.js logging that it was ready. Cause: Docker automatically sets a `HOSTNAME` environment variable to the container id, and Next's standalone `server.js` binds to `process.env.HOSTNAME` — so it was listening on a bogus address instead of `0.0.0.0`/localhost. Fixed with an explicit `ENV HOSTNAME=0.0.0.0` in the frontend Dockerfile's runner stage.

After both fixes, all six services (frontend, backend, worker, scheduler, redis, ollama) reported Docker-healthy, `/health/dependencies` confirmed the backend can reach the real Neon database, Redis, and Ollama, the worker consumed every test job the scheduler enqueued with no drops, and redis/ollama confirmed to have no published host ports.

## 2026-07-20 — Real DATABASE_URL pasted into conversation context

User pasted the real Neon connection string (including password) into this conversation via an `@.env` read, at which point it became part of this session's transcript. Flagged once, in the moment, per `SECURITY.md`; recommended resetting the branch's password in the Neon console at the user's convenience since this is a dev branch (ADR_0006), not production. No further action taken automatically — this is a note for future readers, not an incident requiring rotation before continuing.

## 2026-07-20 — User asked to create database tables before Phase 2; declined and re-sequenced

User asked to create Neon tables directly, ahead of Phase 2/3. Declined per Phase Discipline and the Migrations principle (CONSTITUTION.md): no migration framework exists yet (that's a Phase 4 deliverable, via Alembic), and the typed contracts a schema would encode (provenance records, job envelope, identifiers) don't exist as code until Phase 3. User agreed to follow the roadmap after the tradeoff was explained (AskUserQuestion). No tables were created; Phase 4 will build them properly via Alembic against the same Neon branch.

## 2026-07-20 — Phase 2 (quality enforcement and CI) completed

Tool selection deviated from `TESTING.md`'s original "recommended tooling" list in two places, both logged as implementation decisions rather than ADRs (they don't touch ownership, dependency direction, or any ADR-triggering area per CONSTITUTION.md): Ruff's built-in `C90`/mccabe rule was used for complexity instead of adding Radon as a separate dependency (same metric, one fewer tool to pin); MyPy was chosen over Pyright for type checking (avoids adding a Node-based CLI to an otherwise pip-only CI job). Semgrep was not added — Bandit alone was judged sufficient for Phase 2's baseline; Semgrep can be added later via a normal dependency bump if a concrete gap appears, not pre-emptively.

Real defects found and fixed while actually running every tool against the real code (not assumed clean):

1. **10 real mypy strict-mode errors** — missing generic type parameters on bare `dict` return types (fixed with `TypedDict`s), a stubs gap in `asyncpg` (no `py.typed` marker — acknowledged via a `[[tool.mypy.overrides]]` entry, not a per-line ignore), and `Redis.aclose()` not existing in the installed client's types (switched to the stable `Redis.close()`).
2. **A real bug in `scripts/check_architecture.py`** — its generic-utility-directory check (`shared/`/`common/`/`utils/`/`helpers/`/`misc/`) walked the full tree without the same exclusion list used elsewhere in the script, so it flagged `.venv/`, `.mypy_cache/`, and similar third-party/cache directories as constitutional violations. Fixed by applying `EXCLUDED_DIR_NAMES` consistently and expanding it to cover cache/build directories.
3. **A bug in my own test fixture**, not the script under test — `test_check_size_limits.py` asserted a "review warning" (100-line tier) label on a function that was actually 152 lines, which correctly matches the "strong refactor warning" (150-line) tier first. Fixed the fixture to isolate the tier it claims to test.
4. **Three real dependency CVEs via pip-audit**, against the versions originally pinned: `pip` 25.0.1, `pytest` 8.3.5, and `starlette` 0.46.2 (transitive via FastAPI). Fixed `pip` (upgraded in the Dockerfile) and `pytest`/`pytest-asyncio` (relaxed pins to versions with the fix). `starlette` was the interesting one: bumping FastAPI's pin alone did not pull a patched starlette, because FastAPI's own requirement (`starlette>=0.46.0`) is unbounded and pip's default upgrade strategy doesn't force-upgrade an already-installed transitive dependency that still technically satisfies the range. Fixed by pinning `starlette` directly as a top-level dependency. Re-ran `pip-audit` after each fix — clean at the end, not assumed clean after the first plausible-looking change.

Operational notes (not architecture, but worth keeping so the next person doesn't rediscover them): `docker cp SRC_DIR CONTAINER:DEST_DIR` nests `SRC_DIR` inside `DEST_DIR` if `DEST_DIR` already exists, silently leaving stale files in place — a bind-mounted throwaway container (`docker run -v ...`) was used instead for iterative verification. Git Bash on Windows rewrites arguments like `-w /app` into bogus Windows paths unless `MSYS_NO_PATHCONV=1` is set. Both are now documented in `TESTING.md`'s "Running Checks Locally" section.

Final state: `ruff format`/`ruff check` clean, `mypy --strict` clean (`apps` + `scripts`), both custom checkers clean and unit-tested against synthetic violation fixtures, 12/12 pytest passing, `vulture`/`bandit` clean, `pip-audit` clean, `detect-secrets` found nothing in any git-tracked file, frontend `next lint`/`tsc --noEmit`/`next build` all clean. The GitHub Actions workflow (`.github/workflows/ci.yml`) encodes all of the above as three jobs (`secrets`, `backend`, `frontend`) but has not yet been exercised by an actual push/PR — that's the one verification gap carried into Phase 3.

## 2026-07-20 — Phase 3 (core runtime contracts) completed

PROMPT.md Phase 3 lists "Capability definition," "Permission classification," and "Read and write classification" as core contracts, but CONSTITUTION.md's Core Platform section doesn't name capabilities among its (explicitly non-exhaustive, "Examples:"-prefixed) list. Resolved as: `core/capabilities/` holds the generic capability *contract shape* every domain's capability must conform to (platform-wide, owned by no single domain), while the Capabilities domain owns the actual *registry* — discovery, versioning, cataloging concrete instances — starting Phase 5. Same pattern as `core/events/` holding the generic envelope while domains define their own event payloads later. Not treated as ADR-worthy: it doesn't change any domain's ownership in DOMAIN_OWNERSHIP.md, only decides where a Phase-3-mandated shared type lives within the already-approved `core/` structure.

Also added `core/jobs/` — a job envelope contract, not in the original ARCHITECTURE.md tree. Kept separate from `core/events/` because a job represents work still to be done, while CONSTITUTION.md's Event Principles explicitly exclude "intentions" and "commands" from what an event may represent — reusing the event envelope for jobs would have blurred that distinction. `ARCHITECTURE.md` updated to include it.

Built and verified all eleven Phase 3 deliverables: `core/config` (typed `Settings` via pydantic-settings), `core/time` (`Clock` protocol, `SystemClock`, `FakeClock`), `core/identifiers` (id generation), `core/errors` (18-member taxonomy, each with a stable code/severity/retry classification), `core/logging` (structured JSON with automatic correlation-id injection), `core/observability` (correlation context via `contextvars`), `core/provenance` (`SourceRecord`/`ComputedValueRecord`, matching `DATA_MODEL.md` exactly), `core/events` (generic `EventEnvelope`), `core/jobs` (generic `JobEnvelope`), `core/security` (structured `Permission`, not a bare string), `core/capabilities` (`CapabilityContract` matching `CAPABILITY_REGISTRY.md`'s field list). Refactored all three apps (`api`/`worker`/`scheduler`) to use `core/config` and `core/logging` in place of the Phase 1 ad hoc `os.environ`/`logging.basicConfig` calls, and added correlation-id middleware to the API (honors an inbound `X-Correlation-Id` header, generates one otherwise, returns it in the response).

Real defects found and fixed while actually running the checks and the live stack (not assumed correct):

1. **A real mutable-default-style bug** in `core/provenance/records.py` and `core/events/envelope.py`: `record_id: str = new_id("source")` evaluates the default once at class-definition time, so every instance without an explicit id would have received the *same* id. Fixed with `Field(default_factory=lambda: new_id(...))`. Caught by writing a uniqueness test before assuming the id generation worked, not by a tool.
2. **A real ruff finding** (`B017`): a test used bare `pytest.raises(Exception)` to assert a frozen pydantic model rejects mutation. Fixed by asserting the specific `pydantic.ValidationError` pydantic actually raises.
3. **A real test-isolation bug**: `test_settings_have_sane_defaults` assumed `DATABASE_URL` would be unset, but the verification container runs with `--env-file .env` (so the live stack can actually reach Neon), so pydantic-settings picked up the real value and the assertion failed. Fixed by explicitly `monkeypatch.delenv`-ing every setting the test cares about, so it no longer depends on the ambient environment.
4. **A pytest collection warning**: a class named `TestClock` collided with pytest's default `Test*` discovery convention (it has an `__init__`, so pytest warned it couldn't be collected as a test case). Renamed to `FakeClock` — not just to silence the warning, but because it's a clearer name for what the class actually is (a test double, not a test).

Verified end-to-end in the live stack, not just in isolated tests: rebuilt all containers, confirmed `/health/dependencies` still reaches the real Neon/Redis/Ollama, confirmed the correlation-id middleware both generates a fresh id and honors a supplied `X-Correlation-Id` header, and confirmed structured JSON logs correctly show `correlation_id: null` outside a scope and the real id inside one (worker log during job processing).

Final state: `ruff`, `mypy --strict` (`apps`+`scripts`+`core`), `check_size_limits.py`, `check_architecture.py`, `vulture`, `bandit`, `pip-audit` all clean; 51/51 pytest passing with ~81% coverage (core/ modules mostly 100%, apps/ lower since the health-check dependency paths and worker/scheduler loops aren't exercised by unit tests — acceptable for Phase 3 scaffolding, matching the Constitution's own "test coverage: review flag, not build failure" stance). CI workflow updated to scan `core/` alongside `apps/`.

## 2026-07-20 — Phase 4 (database and repository foundation) completed

Built `infrastructure/database/` (session management + transaction boundaries in `engine.py`, ORM tables in `tables/`, repositories in `repositories/`) and `echo/migrations/` (Alembic, using the async-engine recipe so migrations reuse the app's own asyncpg driver rather than adding a second sync-only driver dependency). Six cross-cutting platform tables, matching Phase 4's explicit list: `audit_events`, `jobs`, `source_records`, `computed_value_records`, `model_calls`, `tool_calls` — all owned by System/Capabilities per `DATA_MODEL.md`, none of them domain-specific (no domain exists yet to own a table).

**The significant real bug this phase**: `asyncpg.connect() got an unexpected keyword argument 'sslmode'`. Neon's connection string carries libpq-style query parameters (`sslmode=require`, `channel_binding=require`); SQLAlchemy's asyncpg dialect forwards URL query parameters verbatim as keyword arguments to `asyncpg.connect()`, which has no `sslmode` parameter (asyncpg uses `ssl=`, passed via `connect_args`). This blocked `alembic revision --autogenerate` on the very first attempt. Fixed in `infrastructure/database/engine.py`: strip query parameters from the URL entirely (`to_asyncpg_url`) and translate `sslmode` into asyncpg's own `ssl=` connect arg explicitly (`wants_ssl`). This is exactly the kind of Neon-specific compatibility detail that would have silently blocked every future phase touching the database if left undiscovered — found by actually running the migration against the real Neon branch, not by reading documentation.

That fix initially only lived in `engine.py`; `migrations/env.py` had its own separately hand-written (and un-fixed) URL-cleaning logic, so the same `sslmode` error reproduced identically on the *second* attempt after the first fix. Rather than patch both copies, `migrations/env.py` was rewritten to call `infrastructure.database.engine.create_engine()` directly — one place owns Neon connection-string handling, not two copies that can silently drift (`CONSTITUTION.md`: Duplicate Business Rules applies to infrastructure code too, not just domain rules).

Verified against the real Neon dev branch end to end, not assumed correct from the code alone:
- `alembic revision --autogenerate` correctly detected all six tables and their indexes from the ORM models.
- `alembic upgrade head` applied cleanly to the empty database.
- `alembic downgrade base` then `upgrade head` again — full reversibility confirmed, not just assumed from the generated downgrade function.
- All six tables' contents queried directly via `information_schema` and confirmed present.
- Integration tests (24 new tests: audit, provenance, jobs, observability repositories, plus two dedicated transaction-boundary tests) ran against the real branch — 63/63 total passing.
- After the full test run, every table was queried again and confirmed to contain zero rows — the test-isolation strategy (uncommitted sessions rolled back on teardown; the one deliberately-committing test cleans up explicitly) leaves no residue on the real dev branch.
- The transaction-boundary tests specifically prove `session_scope`'s contract: one test inserts a row then deliberately raises inside the scope and confirms the row does not exist afterward (rollback); another commits for real, confirms the row exists, then deletes it (commit path, with explicit cleanup since this path doesn't self-discard).
- Confirmed no code in `infrastructure/` or `migrations/` logs anything (grepped for `logger.`/`print(`) and that SQLAlchemy's own SQL-echo logging is never enabled — satisfies Phase 4's "sensitive values are not logged" criterion concretely, not by assertion.
- Rebuilt the live Docker stack (Dockerfile now copies `infrastructure/`, `migrations/`, and `alembic.ini`) and ran `alembic current` inside the *deployed* `backend` container (not the bind-mounted dev container) to confirm the image itself is self-sufficient for running migrations in a real deployment, not just in the verification setup.

`tests/integration/` skips gracefully (not a failure) when `DATABASE_URL` isn't set, since no database secret is configured in GitHub Actions yet — CI runs everything else; integration tests are verified locally against the real Neon branch, per `TESTING.md`'s updated "Running Checks Locally" section.

## 2026-07-21 — Continuing autonomously through phases per explicit user instruction

User asked to keep working through phases until usage hits 85%, committing after each phase but not pushing, with all other rules/Constitution still applying. This supersedes the default "stop and wait for explicit instruction between phases" behavior for the phases covered by this instruction — each phase below still gets its own restated objective, verification, and completion report; only the "wait for a new go-ahead message" step is what's being skipped, at the user's explicit request.

## 2026-07-21 — Phase 5 (capability registry and tool execution) completed

First real domain: `domains/capabilities/` (models, errors, policies, service — no `repository.py` yet, since the registry is in-process/code-populated for Phase 5, not dynamically edited at runtime; only tool-call *execution audit* is persisted, via the `ToolCallRepository` already built in Phase 4). `CapabilityRegistry` is registration-only discovery (no keyword lists); `CapabilityExecutor` runs the full pipeline from `CAPABILITY_REGISTRY.md` — lookup, permission check, input validation, execution (with timeout), output validation — with every attempt audited exactly once in a `finally` block, so a failure at any stage still produces a tool-call record.

Write capabilities are registrable (their `CapabilityContract` can declare `read_write_classification=WRITE`) but the executor refuses to run them (`WriteCapabilityNotExecutableError`) until the Approval Engine exists — this is Phase 6's explicit prerequisite relationship from PROMPT.md, enforced in code now rather than left as a documentation-only promise.

Built five fake test capabilities (echo, permission-gated echo, write, slow/timeout, always-failing) per the Phase 5 deliverable list's explicit call for "fake test capabilities," plus a `FakeToolCallRepository` for fast unit tests. One integration test wires the executor to the *real* Postgres-backed `ToolCallRepository` to prove the Protocol contract holds against the real implementation, not just the fake.

All five of Phase 5's verification criteria were written as tests, not just claimed: invalid input never reaches the handler (proven with a spy handler that asserts it was never called), a capability cannot run without its required permission, every execution attempt is audited including failures, discovery requires no keyword list, and (via the architecture checker plus the fact that the executor only ever touches typed Pydantic models) no provider-specific data crosses a domain boundary — there are no real providers yet, so this is necessarily a structural argument rather than an empirical one, which is the honest state of things at this phase.

One real gap found during verification, not a code bug but a coverage-tooling gap: `domains/` had been created but never added to `pyproject.toml`'s coverage/vulture/bandit path lists or the CI workflow/`TESTING.md` runbook, so the new domain code was silently invisible to every quality gate. Fixed by adding it everywhere the other three packages (`apps`, `core`, `infrastructure`) already appeared — a good reminder to update these lists as a checklist item whenever a new top-level package is introduced, not just when writing the code itself.

Final state: `ruff`, `mypy --strict` (`apps`+`scripts`+`core`+`infrastructure`+`migrations`+`domains`), both custom checkers, `vulture`, `bandit`, `pip-audit` all clean; 83/83 tests passing (63 carried over + 20 new), 89% overall coverage, `domains/capabilities` at 99-100%.

Also caught (not a Phase 5 defect, an omission from Phase 4/5 discipline): the live Docker stack hadn't actually been rebuilt since Phase 4, so it was running stale code through both Phase 5 and into Phase 6 — the Dockerfile is supposed to be updated and the stack rebuilt every phase that adds a new top-level package, and this slipped. Caught and fixed at the start of Phase 6's verification (see below) rather than left to compound further.

## 2026-07-21 — Phase 6 (central approval engine) completed

`domains/approvals/` implements `APPROVAL_MODEL.md`'s state machine exactly (11 states, transitions rejected in code via `VALID_TRANSITIONS`). Unlike Capabilities, this domain owns real persisted state — proposals and approval decisions are domain-owned aggregate roots (CONSTITUTION.md: Aggregate Ownership), so — a deliberate deviation from the Phase 4 pattern — the ORM tables (`ApprovalProposalRow`, `ApprovalDecisionRow`) live inside `domains/approvals/repository.py` itself, not under `infrastructure/database/tables/`, which stays reserved for tables no domain owns. `migrations/env.py` updated to import from both locations so Alembic autogenerate sees every table regardless of which layer it lives in.

All seven of Phase 6's verification criteria are tests, not assertions:
1. **Execution without approval fails** — `execute()` on a merely-submitted (not approved) proposal raises `ApprovalRequiredError`.
2. **Self-approval is impossible** — `approve()` rejects `SYSTEM_ACTOR` as the approving identity with `SelfApprovalNotAllowedError`, enforced in code, not just by convention.
3. **Approval binds to the exact payload** — the `ApprovalDecision` records the payload hash at approval time; a second test tampers with the stored proposal's payload directly (bypassing the intended `edit()` path, to prove the *defense* works even if some future bug skips the front door) and confirms `execute()` still catches the mismatch via `PayloadMismatchError`.
4. **Editing invalidates approval** — `edit()` cancels the old proposal and mints a new `proposal_id`; the new one requires its own fresh approval before it can execute.
5. **Expired approval fails** — using `FakeClock.advance()` past the approval TTL, `execute()` raises `ApprovalExpiredError`.
6. **Duplicate execution is prevented** — calling `execute()` twice on the same already-executed proposal returns the existing result idempotently; the fake write adapter's call count proves it never runs twice.
7. **Failed verification produces the correct state** — a verifier configured to return `False` drives the proposal to `verification_failed` (not silently treated as success), and `execute()` raises `ExecutionUncertainError` rather than returning normally.

A fake external write adapter (`FakeWriteAdapter`) is used per Phase 6's explicit instruction, configurable to succeed or raise, so both the execution-failure and success paths are tested without any real provider integration existing yet.

Real defects found and fixed during verification, not assumed correct: (1) the live Docker stack had silently gone stale since Phase 4 — the Dockerfile wasn't updated to `COPY domains` when Phase 5 introduced it, so the deployed containers had been running Phase-4-era code through all of Phase 5 and into Phase 6's start; caught by remembering to actually rebuild and check `alembic current` in the *deployed* container, not just the dev verification container. (2) `vulture` flagged `ExecutionVerifier.verify`'s parameter as an unused variable — a real false positive, since it's a `Protocol` stub body vulture doesn't understand is meant to be unimplemented; fixed by making the parameter positional-only (`/`) and underscore-prefixed, vulture's own documented convention for "intentionally unused," rather than suppressing the tool or leaving the gap unexplained.

Final state: `ruff`, `mypy --strict`, both custom checkers, `vulture`, `bandit`, `pip-audit` all clean across `apps`+`core`+`infrastructure`+`domains`+`migrations`+`scripts`; 97/97 tests passing (83 carried over + 14 new: 13 unit + 1 integration against real Postgres), 91% overall coverage. Live stack rebuilt, `alembic current` confirmed at head in the deployed backend container, zero row residue in any table after the full test run.

## 2026-07-21 — Phase 7 (model gateway) completed

Before writing the cost-estimation table, verified current Claude API pricing via web search against anthropic.com directly (CONSTITUTION.md: Provider Due Diligence — "Marketing material shall not be treated as authoritative technical documentation," and more specifically here, *invented* numbers weren't an option at all). Found: Sonnet 5 at $2/$10 per million tokens (input/output) as an introductory price through 2026-08-31, reverting to $3/$15 after; Haiku 4.5 at $1/$5; Opus at $5/$25. Recorded the 2026-08-31 expiry directly in `providers/models/claude/pricing.py`'s docstring as dated, tracked technical debt (CONSTITUTION.md's Technical Debt section: "time bounded... tracked for future resolution") rather than a silent future surprise.

Built `providers/models/`: a common `ModelRequest`/`ModelResponse` contract, a real Claude adapter (`anthropic` SDK), a real Ollama adapter (plain `httpx` against Ollama's REST API — no new dependency, reusing the same approach already proven in the Phase 1 health check), a deterministic escalation policy, and a `ModelGateway` that picks the adapter via `core.config.Settings.default_model_provider` (a plain string, not the `Provider` enum, so `core/` never imports from `providers/` — dependency direction preserved) and validates structured output against a caller-supplied Pydantic model before returning it.

**Verification is asymmetric between the two adapters, and that asymmetry is recorded rather than hidden.** Ollama already runs in the Compose stack with no credentials required, so its adapter got a genuine live test: pulled a small model (`smollm2:135m`, chosen for fast download over capability, since only the plumbing needed proving) into the running `ollama` container and called the real adapter against it — passed. Claude requires an API key that doesn't exist in this environment, so its adapter is verified by mocking the `anthropic` SDK's client (`AsyncMock` standing in for `messages.create`) rather than a live call — this proves the adapter's translation logic (SDK response → `ModelResponse`, SDK error → `ProviderUnavailableError`) but not that a real Claude call actually succeeds end-to-end. That gap is real and stays open until a key is provided; it is not glossed over as "tested."

Real defects found and fixed:
1. **A genuine mypy strict-mode finding**, not a false positive: `block.text for block in message.content if getattr(block, "type", None) == "text"` — the `getattr` check doesn't type-narrow for mypy, since `message.content` is a large union of Anthropic content-block types and only `TextBlock` has `.text`. Fixed with `isinstance(block, TextBlock)`, which does narrow, and updated the corresponding test fixture from a `SimpleNamespace` stand-in (which can't satisfy `isinstance`) to a real `TextBlock` instance.
2. **The exact same coverage-tooling gap as Phase 5, recurring** — `providers/` existed but wasn't added to `pyproject.toml`'s coverage/vulture/bandit paths, the CI workflow, or `TESTING.md`'s runbook, despite Phase 5's own decision log entry explicitly flagging this as a checklist item for "whenever a new top-level package is introduced." Fixed the same way, and this note exists so a third recurrence is less likely — the actual fix is to treat "add the new top-level package to every tool path list" as a standing step in the phase-completion checklist, not just something to remember after the fact.
3. **The live Docker stack rebuild step got interrupted mid-command** (the background build task was marked "killed" partway through, though the image build itself had already completed by that point) — caught by checking the actual task output rather than trusting the status label, then running `docker compose up -d` separately to bring the already-built images up.

Added a new architecture rule, `providers-must-not-import-domains` (the reverse of Phase 3's `domains-must-not-import-providers`), giving `check_architecture.py` a structural proof for Phase 7's "model calls cannot directly execute external actions" verification criterion: a model adapter has no import path to any domain's execution logic, so it is architecturally incapable of calling `domains.approvals` or `domains.capabilities` directly, regardless of what a model itself might request in its own output text.

Final state: `ruff`, `mypy --strict`, both custom checkers, `vulture`, `bandit`, `pip-audit` all clean across `apps`+`core`+`infrastructure`+`domains`+`providers`+`migrations`+`scripts`; 120/120 tests passing (97 carried over + 23 new, including one live Ollama call), 92% overall coverage. Live stack rebuilt and confirmed healthy with `providers/` included in the deployed image.

---

## 2026-07-21 — Phase 8 (conversation loop and provenance grounding) completed

Built `domains/conversation/` (sessions/messages, domain-owned persistence per the Approvals precedent), the first real capability (`application/capabilities/current_time.py`), and `application/orchestrators/conversation.py` — a minimal Request Lifecycle: user message -> Capability Planner (structured yes/no model call: "does this need the current time?") -> Capability Executor -> evidence -> Response Generator -> persistence. Wired through `apps/api/routes/conversations.py` (start/send/send-stream/history) and a minimal `frontend/app/chat/page.tsx`.

This phase's real risk was never the plumbing — capability execution, evidence gathering, and persistence all worked the first time and stayed working, confirmed by both fakes-based unit tests and live calls. The risk PROMPT.md actually cares about here is the one CONSTITUTION.md calls out by name: a language model asked "what time is it" defaulting to its own training-data guess instead of the system clock it was just handed. Proving that required live testing against the real local model, not just fakes — and live testing surfaced two genuine, unrelated defects that fakes-based tests could not have caught, because both are properties of what `llama3.2:1b` actually does with specific wording, not of the orchestration code around it.

**Defect 1 — the evidence-grounded response refused to answer.** Given `_RESPONSE_PROMPT_WITH_TIME`'s original wording ("The current UTC time, from the system clock (not your own knowledge), is {ts}... Respond to: {message}"), the live API consistently returned "I can't provide real-time information, including the current date and time" — ignoring the timestamp it had just been given. An isolated manual call to the same adapter with what looked like the same prompt had *succeeded*, which was the misleading part: at default (randomized) sampling, the correct answer was just one of several samples the model could produce, not what it reliably produces. Lowering `temperature` did not fix this — it made it worse, converging deterministically on the refusal. That result was the actual diagnosis: the refusal was the model's single highest-probability completion for that wording, at any temperature; higher temperature's occasional correct answer was luck, not a signal that the prompt was fine. The fix was rewording, not resampling: reframing the injected fact as something the model already has ("[SYSTEM FACT] The current date and time is {ts}... Do not say you lack real-time access — you have just been given the real-time value above") rather than something being relayed to it from a live lookup, which is what had been triggering the model's trained "I don't have real-time access" refusal reflex. Verified deterministically correct (`temperature=0.0`) across three different message phrasings, 3 runs each (9/9), then confirmed live through the actual API repeatedly.

**Defect 2 — the Capability Planner always said yes.** Found while spot-checking a non-time question ("tell me a joke about cats") through the live API and noticing it got current-time evidence injected anyway. Direct testing of `_PLANNER_PROMPT` against `llama3.2:1b` showed it was not discriminating on message content at all: `{"needs_current_time": true}` for every message tested, including "what is 2+2" and "write me a haiku," deterministically, at `temperature=0.0`. This is a materially different failure than Defect 1 — not a refusal reflex, but the model not attending to the actual classification task at 1B-parameter scale from a zero-shot instruction alone. Fixed by rewriting the prompt as a five-example few-shot classifier rather than a bare instruction; re-tested against 11 messages across both categories (jokes, arithmetic, haiku, cooking, literature vs. clock/date/calendar/market-hours questions) and got the correct classification every time. This is the kind of small-model reliability gap CONSTITUTION.md's escalation policy exists for — Phase 8 does not add escalation-on-low-confidence itself, but the finding is recorded here as the concrete motivating case for it.

Neither defect was hypothetical or found by inspection — both were reproduced through the real running stack (`docker compose`, real Ollama, real Postgres, real HTTP calls), root-caused with direct adapter calls that isolated the variable in question (wording vs. sampling for Defect 1; prompt structure vs. model capability for Defect 2), and re-verified the same way after the fix. `ModelRequest` gained an optional `temperature` field (both adapters honor it; `None` preserves each provider's own default) as part of Defect 1's fix — it is used narrowly, only for the planner's classification call (`0.0`) and the evidence-grounded response call (`0.1`, not `0.0`, since determinism converging on a *wrong* answer is exactly as bad as never being deterministic at all — low-but-nonzero was verified, not assumed, to still land on the correct answer across repeated runs).

Verification, all six PROMPT.md Phase 8 criteria, confirmed live against the actual running stack: (1) user asks for the current time via the real HTTP API; (2) the real `system.current_time` capability fires (confirmed via the `evidence` field, not inferred); (3) the response states the exact injected timestamp, not a paraphrase or a guess; (4) the model never falls back to its own training-data date, and — the defect this phase actually found — never refuses either; (5) `GET /conversations/{id}/messages` returns full, correctly-ordered history; (6) `docker compose restart backend` does not lose conversation data (checked message count before and after: 6 and 6).

Final state: `ruff format --check`, `ruff check`, `mypy` (0 issues across 90 source files), `check_architecture.py`, `check_size_limits.py`, `vulture`, `bandit` all clean across `apps`+`core`+`infrastructure`+`domains`+`providers`+`application`+`migrations`+`scripts`; 134/134 tests passing (120 carried over + 14 new, including two live-Ollama-backed integration tests), 90% overall coverage. Live stack rebuilt twice (once per defect fix) and re-verified against the running containers each time, not just against the test suite.

---

## 2026-07-21 — Phase 9 (Memory foundation) completed

Built `domains/memory/` (the CANDIDATE -> CONFIRMED -> {SUPERSEDED | EXPIRED | DELETED} lifecycle from CONSTITUTION.md's Memory Lifecycle diagram, condensed to the states Phase 9 actually needs — "Review" and "Usage" are policy/consumption concepts, not separate persisted statuses, so they aren't given their own enum members per No Future Scaffolding), plus `application/orchestrators/memory_extraction.py` (extraction using the Model Gateway, same Application-layer placement as Phase 8's conversation orchestrator, for the same reason: cross-domain coordination belongs there, not inside a domain).

**Research before implementation.** DOMAIN_OWNERSHIP.md's Memory section is the authoritative ownership description (per this project's own earlier resolution: DOMAIN_OWNERSHIP.md wins when it and CONSTITUTION.md's illustrative examples disagree). One real, previously-uncaught inconsistency surfaced doing that research: CONSTITUTION.md's "Example Events" list includes `MemoryCandidateCreated` and `MemorySuperseded`, neither of which appears in DOMAIN_OWNERSHIP.md's authoritative Memory event catalog (`MemoryCreated, MemoryUpdated, MemoryArchived, MemoryDeleted, MemoryPromoted, MemoryConfidenceChanged, MemoryConflictDetected`) or in DOMAIN_EVENTS.md's own catalog row, which matches DOMAIN_OWNERSHIP.md. Per the existing precedent, this is noted as a known, non-blocking documentation inconsistency rather than fixed by editing CONSTITUTION.md's illustrative list — no code depends on it, since **no domain in this codebase publishes events yet** (verified: `core/events/envelope.py` is a wire-format contract with no publisher/subscriber wiring anywhere, and Approvals/Conversation don't publish either). Memory does not publish events in this phase either, for the same No Future Scaffolding reason nothing else does — this is recorded so it isn't mistaken for an oversight later.

**A real, undocumented-until-now scope decision:** DOMAIN_OWNERSHIP.md says "Memory writes follow the Approval Engine when required by constitutional policy," but no passage in CONSTITUTION.md actually specifies which Memory writes that means. Decided *not* to route memory confirmation through the Central Approval Engine this phase — Approvals' own documentation (APPROVAL_MODEL.md) describes its first real users as Calendar (Phase 11) and Email (Phase 21), both external-system side-effecting writes; confirming an internal memory record has no external effect to approve. If a future phase decides memory promotion should require human approval (e.g. before it's used to ground responses), that is a real design question for that phase, not retrofitted here.

**Live-testing found two real, distinct small-model reliability problems** in `application/orchestrators/memory_extraction.py`, on top of Phase 8's already-learned lessons:

1. **A single combined "decide-and-extract" prompt was unusable.** The first design asked `llama3.2:1b` to decide *and* extract a candidate fact in one call. Live testing against seven real messages found it hallucinating a fake low-confidence fact for "What time is it?" and, on "Can you write me a haiku?", echoing one of the prompt's own few-shot examples back verbatim instead of processing the actual message — a different, more severe failure mode than the always-true planner bug Phase 8 found. Fixed by splitting into two calls: a binary few-shot gate ("does this message state a durable fact?", reusing Phase 8's proven Capability Planner pattern) followed by a separate extraction call only when the gate says yes. This measured far more reliably.
2. **The gate's few-shot generalization is narrow, and more examples are not a free improvement.** A live end-to-end test asked "What is the current date and time?" and got a false-positive fact extracted. Adding that exact phrase as a 7th few-shot example didn't fix it in isolation — it measurably broke five of the six previously-correct examples, causing the model to default to "true" almost universally. Swapping the new example in place of an existing one instead fixed the new phrase but broke three different previously-correct cases. This is strong evidence the model is closer to matching against the literal example set than learning the general rule, at this parameter count — a real, live-tested finding, not a hypothesis. Kept the original six-example gate (proven reliable across eleven distinct test messages) rather than chasing full phrasing coverage, and documented the limitation directly in the orchestrator's own docstring and in the test that exercises it, choosing a test phrasing within the model's demonstrated reliable range instead of one at its boundary.

**A tooling mistake, caught and fixed, worth recording so it isn't repeated:** `docker compose cp CONTAINER:SRC DEST` nests `SRC`'s contents inside `DEST` when `DEST` already exists locally, rather than overwriting it in place. Used naively while syncing edited files back from the container mid-phase, this silently left a stale, never-imported `domains/memory/memory/*.py` duplicate on disk (dead weight that could have been committed by accident) and meant a `ruff format` pass appeared not to have taken effect on two test files, because the reformatted copies were never actually written back to their real path. Fixed by always either deleting the destination first or using the `SRC/.` trailing-dot form, and re-verified there was no other stray nested directory anywhere else in the tree before the final verification pass.

Verification, all four PROMPT.md Phase 9 criteria, confirmed both by direct unit tests against `MemoryService` (`tests/unit/domains/memory/test_memory_service.py`, one test per criterion, named after the criterion) and live against the real running API: (1) `POST /memory/extract` only ever creates `CANDIDATE` records — confirming one is a distinct, explicit `POST /memory/{id}/confirm` call; (2) `GET /memory/{id}/conflicts` correctly found the "blue" memory as a conflict of a later "green" memory for the same `subject_key`, both live and in tests; (3) `DELETE /memory/{id}` removed a memory from `GET /memory/search` while it remained visible (with `status=deleted`) in the full `GET /memory` user view — checked live, not just asserted in a fake-backed test; (4) every record's `source_type`/`source_id`/`correlation_id` round-tripped through both the Postgres repository and the live API responses.

Final state: `ruff format --check`, `ruff check`, `mypy` (0 issues across 100 source files), `check_architecture.py`, `check_size_limits.py`, `vulture`, `bandit`, `pip-audit` all clean; 166/166 tests passing (134 carried over + 32 new, including three live-Ollama-backed extraction tests and Postgres-backed repository tests), 91% overall coverage, `domains/memory/` itself at 100%. Live stack rebuilt and re-verified against the running containers (full extract -> confirm -> conflict -> search -> delete -> supersede lifecycle exercised over real HTTP, plus a `docker compose restart backend` persistence check), not just against the test suite.

---

## 2026-07-21 — Phase 10 (Google Calendar read integration) completed

Built `domains/calendar/` (a `CANDIDATE`-free domain, unlike Memory — Calendar's own read data has no confirmation lifecycle, just credentials and a synced-event cache), `providers/calendar/google/adapter.py` (plain `httpx` against Google's REST APIs, no `google-api-python-client`/`google-auth` SDK, matching the Ollama adapter's precedent), and `infrastructure/secrets/encryption.py` (a new piece of platform infrastructure — Fernet-based encryption at rest for OAuth tokens, required by `Docs/SECURITY.md` and never built before this phase since no prior phase stored a real external secret).

**Provider Due Diligence, done for real before writing any adapter code.** Google's OAuth2 web-server flow and Calendar API v3 endpoints (authorization/token URLs and parameters, the Event resource's exact field shapes, `singleEvents=true`'s recurring-instance expansion, the `freeBusy.query` request/response shape) were fetched and read directly from `developers.google.com` before `providers/calendar/google/adapter.py` was written, not reconstructed from training-data memory of what Google's API "probably" looks like — consistent with `CONSTITUTION.md`'s "Marketing material shall not be treated as authoritative technical documentation," extended here to any vendor API surface, not just pricing.

**A real architecture-layering question, resolved by precedent rather than by inventing a new pattern.** `CalendarProviderPort` (the Protocol `CalendarService` depends on) speaks entirely in raw dicts — Google's actual JSON responses — never in a typed domain object, exactly matching `domains/approvals/service.py`'s `WriteAdapter.execute(payload) -> dict[str, Any]` precedent from Phase 6. This was the only way to keep `providers/calendar/google/adapter.py` from ever importing `domains/calendar/` (`scripts/check_architecture.py`'s `providers-must-not-import-domains` rule): the domain owns translation (`domains/calendar/policies.py`'s `parse_event`/`parse_free_busy`/`parse_calendar_list`), the provider only ever returns primitives. Constructing the concrete adapter still needed `application/calendar_provider_factory.py`, mirroring Phase 8's `application/model_gateway_factory.py`, since `apps/api/dependencies.py` cannot import `providers/` directly either (`apps-must-not-import-providers`) — this is now the second time this exact factory pattern has been needed, confirming it's a real, recurring architectural shape rather than a one-off.

**A genuine new requirement neither Approvals, Conversation, nor Memory had: per-request state without a per-request session.** `apps/api/dependencies.py`'s capability registry is built once, `@lru_cache`d, at process start (`current_time`'s own precedent) — but Calendar's two capabilities (`calendar.list_events`, `calendar.free_busy`) need database access, which only exists per-request. Fixed by having each capability handler open its own `session_scope()` internally rather than closing over a registry-construction-time session, making capability execution a self-contained transaction — documented directly in `application/capabilities/calendar_read.py` since this is the first capability to need it and the reasoning isn't obvious from the code alone.

**Encryption at rest, built from nothing.** `Docs/SECURITY.md` requires OAuth tokens be "encrypted at rest," but no encryption infrastructure existed anywhere in the codebase before this phase (`grep -i encrypt` across the whole tree returned zero matches). Added `infrastructure/secrets/encryption.py` (`SecretCipher`, wrapping the `cryptography` package's Fernet — AES-128-CBC + HMAC-SHA256, not hand-rolled crypto) as new cross-cutting platform infrastructure, matching `infrastructure/database/`'s placement rather than being made a provider port, since it's not a swappable vendor integration. `pip-audit` immediately caught a real problem with the first version pinned (`cryptography>=43,<44`): four known advisories on 43.0.3, including `GHSA-537c-gmf6-5ccf` (a vulnerable statically-linked OpenSSL bundled in the wheel), all fixed in 48.0.1 — repinned to `>=48.0.1,<49` and re-verified clean.

**A live, multi-step OAuth debugging chain, each step a genuine finding rather than guesswork:**
1. The first live attempt failed with the browser reporting `ERR_CONNECTION_REFUSED` on the callback, and the backend's own logs showed the callback request never arrived at all — meaning the failure happened between Google and the browser, not in this codebase. Root cause: the user's browser runs on a different machine/network than the one running Docker, so `http://localhost:8000` in the registered redirect URI resolved on their machine, not this one, even though the initial `/oauth/authorize` redirect (a server-side response, not a browser-to-backend connection) worked fine.
2. Fixed by tunneling this machine's port 8000 to a public HTTPS URL with a Cloudflare quick tunnel (`docker run cloudflare/cloudflared tunnel --url http://host.docker.internal:8000` — no local install, no account needed) and updating `GOOGLE_OAUTH_REDIRECT_URI` to match.
3. The retry failed differently: Google rejected the redirect URI outright. Root cause, verified against Google's own OAuth docs rather than assumed: the existing OAuth client was type "Desktop app," which does not support registering an arbitrary custom HTTPS redirect URI the way a "Web application" client does (Desktop clients are designed for loopback/native-app flows). Since this backend is a genuine server holding a `client_secret` behind a real HTTPS callback, "Web application" is both the correct and Google-recommended type — not something fixable in code. The user created a new Web-application-type OAuth client; the flow succeeded on the next attempt.
4. Live testing against the connected real account then surfaced a fourth, unrelated, genuine bug: every route across *all* phases (Conversation, Memory, Calendar alike) was returning a bare, bodyless HTTP 500 for any domain (`EchoError`) failure — no prior phase's live testing had exercised a real failure path through the actual HTTP layer to notice this. Fixed by adding an `http_status: int` class attribute to `EchoError` and every subclass (mirroring the existing `code`/`retryable`/`severity` per-class pattern) and registering one global `@app.exception_handler(EchoError)` in `apps/api/main.py` that every route now benefits from, not just Calendar's — closing a real gap in Phases 6-9's own verification, not just Phase 10's.

Verification, all six PROMPT.md Phase 10 criteria, confirmed live against the real connected Google account (`adamtkelly47@gmail.com`), not synthetic fixtures: (1) real events retrieved from the user's actual "Holidays," "Family," and primary calendars via `GET /calendar/events`; (2) a real recurring "Jake Gymnastics" event returned with `recurring_event_id` correctly linked to its parent series; (3) its `America/Los_Angeles` timezone was correctly parsed and UTC-converted (`17:00Z`-`21:00Z` for a 10am-2pm Pacific event); (4) forcibly expiring the stored token in Postgres (setting `access_token_expires_at` to 2020) and re-requesting caused a real, live refresh against Google's token endpoint, with the new expiry persisted and the request still succeeding transparently; (5) both a missing-credential request and a request for a nonexistent event (a real Google 404) now return honest, typed error bodies (`404 calendar_credential_not_found`, `502 provider_unavailable`) instead of a bare 500; (6) the credential's stored `scope` — read back from what Google actually granted, not just what was requested — is exactly `https://www.googleapis.com/auth/calendar.readonly`.

A scope decision, made and recorded rather than left implicit: Calendar's write phase (11) does not exist yet, and `Docs/APPROVAL_MODEL.md` names Calendar as one of the Approval Engine's first two intended users — this phase adds no write path of any kind (no create/modify/delete event capability, no proposal type), consistent with `Docs/SECURITY.md`'s "read scopes ... write scopes are never requested until the corresponding write phase."

Final state: `ruff format --check`, `ruff check`, `mypy` (0 issues across 116 source files), `check_architecture.py`, `check_size_limits.py`, `vulture`, `bandit`, `pip-audit` all clean; 207/207 tests passing (166 carried over + 41 new), 89% overall coverage. Live stack rebuilt multiple times across the OAuth debugging chain and the error-handling fix, each rebuild re-verified against the actual running containers and the actual connected Google account, not assumed fixed from the diff alone.

---

## 2026-07-21 — Phase 11 (Calendar approval-gated writes) completed

Before starting, explicitly asked the user for confirmation that live verification would create/modify/delete a real event on their real connected Google Calendar — this phase is categorically different from every prior one (Phases 1-10 only ever read data or touched our own sandboxed Postgres), and CONSTITUTION.md's own caution about hard-to-reverse, real-world-side-effect actions applies here even under the standing "continue through phases" instruction. Confirmed before proceeding.

Built `domains/calendar/write_adapters.py` (`CalendarCreateEventWriteAdapter`/`CalendarModifyEventWriteAdapter`/`CalendarDeleteEventWriteAdapter`/`CalendarWriteVerifier`/`CalendarDeleteVerifier` — concrete implementations of `domains/approvals/service.py`'s `WriteAdapter`/`ExecutionVerifier` Protocols, exactly as that file's own Phase 6 docstring anticipated: "real provider adapters ... implement this same Protocol starting with each integration's own phase") and `application/orchestrators/calendar_writes.py` (`CalendarWriteOrchestrator`, coordinating Calendar + Approvals — the Application layer's job). No new database migration was needed: Approvals' existing `approval_proposals`/`approval_decisions` tables (Phase 6) already store an arbitrary `action_type` and JSONB `payload`, which is exactly what a calendar write proposal is.

**The OAuth scope had to change, and existing connections had to be re-authorized.** `providers/calendar/google/adapter.py`'s requested scope moved from `calendar.readonly` alone to `calendar.readonly` + `calendar.events` (verified against Google's own scope list: `calendar.events` grants "view and edit events on all your calendars," not the broader `calendar` scope, which also permits deleting entire calendars — CONSTITUTION.md: Least Privilege). Google does not retroactively upgrade an already-granted token's scope, so the user had to redo the OAuth consent flow — including, again, standing up a fresh Cloudflare quick tunnel, since the previous session's tunnel URL had expired and the browser is still on a different machine than this one.

**Recurring event edit scope, verified against Google's real documented behavior rather than assumed:** `developers.google.com/calendar/api/guides/recurringevents` confirms the API natively supports exactly two scopes — PATCH/DELETE a single instance's own event id (an exception to the series), or PATCH/DELETE the parent recurring event's id (the whole series). "This and following instances," a scope many calendar UIs offer, is *not* a native API operation — Google's own guide describes it as a two-call workaround (truncate the original series with an `RRULE` `UNTIL`, then insert a new series). Deliberately not implemented: PROMPT.md Phase 11's objective is "narrow, safe calendar modifications," and a two-call, partially-completable operation with its own distinct failure mode (call 1 succeeds, call 2 fails, leaving the series truncated with no replacement) is neither narrow nor safe. `domains/calendar/models.py`'s `RecurringEditScope` enum has exactly two members, and a caller must pick one explicitly — there is no default.

**A second genuine "no native support" finding, this one about deletion itself:** live testing (and confirmed via Google's own documentation before writing `CalendarDeleteVerifier`) showed that deleting an event does not 404 the event out of existence — Google marks it `status: "cancelled"` and a `GET` still returns it (with reduced detail, per Google's docs, "only guaranteed to have the id field populated" once cancelled). `events.list`'s default behavior excludes cancelled events, which is why they still disappear from normal listings. This directly shaped `CalendarDeleteVerifier` (verifies `status == "cancelled"` on reload, not that the request 404s) and `domains/calendar/repository.py`'s cache eviction (deletes the local cache row outright on a successful delete, rather than trying to track a "cancelled" status the read path has no other reason to represent).

**A real gap discovered by trying to wire the cache-sync step, not by inspection:** `domains/approvals/service.py`'s `ApprovalService.execute()` returns only the updated `ActionProposal` — it does not expose the write adapter's raw result to its caller (a deliberate Phase 6 design: the proposal's own state is the public contract). But updating Calendar's local event cache after a successful create/modify needs that raw result (the newly-assigned event id, in particular, for `create_event` — there's no other way to know it). Rather than changing Phase 6's tested code to accommodate this, `application/orchestrators/calendar_writes.py` wraps the real `WriteAdapter` in a thin `_CapturingWriteAdapter` that records the last result as a side effect of `execute()` — `ApprovalService` gets exactly the Protocol it expects, and the orchestrator can still see what actually happened.

Verification, all five PROMPT.md Phase 11 criteria, confirmed live against the real connected Google account: (1) a proposed create event did not appear on the real calendar until explicitly approved and executed — checked via a live `GET /calendar/events` before approval; (2) the full payload (summary, description, exact Google-shaped `start`/`end` nodes) was returned verbatim by `GET /approvals/{id}`, not summarized; (3) re-executing the same already-executed proposal (the Approval Engine's own Phase 6 idempotency guarantee — `execute()` short-circuits on a terminal status rather than re-running the adapter) was confirmed to create no second event on the real calendar — one event existed before and after; (4) every create, modify, and delete came back `status: "executed"` (not `verification_failed`) only after a fresh, independent re-`GET` from Google confirmed the result — for delete specifically, confirmed the reloaded event actually shows `status: "cancelled"`; (5) a `scope=entire_series` request with no `recurring_event_id` is rejected before ever reaching Google (`ValidationError`), and single-instance vs. entire-series modification was proven to target the correct event id in both fakes-based tests and structurally in the real live create -> modify -> delete cycle (the live cycle used a non-recurring test event deliberately — the user's real recurring family-calendar events were not used as a live test subject, to avoid touching real, meaningful personal data for a check the unit tests already cover deterministically).

Final state: `ruff format --check`, `ruff check`, `mypy` (0 issues across 120 source files), `check_architecture.py`, `check_size_limits.py`, `vulture`, `bandit`, `pip-audit` all clean; 229/229 tests passing (207 carried over + 22 new), 88% overall coverage. Live stack rebuilt and re-verified against the actual running containers and the actual connected Google account through a full create -> review -> approve -> execute -> duplicate-execute -> modify -> delete -> verify-cancelled cycle, with the test event cleaned up (cancelled) at the end.

---

## 2026-07-21 — Phase 12 (Schwab read integration) completed

Explicitly asked the user for confirmation before starting, since Phase 12 is the first real financial-account integration (real balances, positions, a trade-capable OAuth token) — the same "check before an action with real-world consequences" pause applied in Phase 11, extended here to a more sensitive account category. Also flagged, and got explicit confirmation on, a real Schwab-specific constraint discovered before writing any code: unlike Google, Schwab has no separate read-only OAuth product — the grant is trade-capable regardless, and Echo's own code is the only enforcement boundary (PROMPT.md Phase 12 verification 5: "no trading endpoint is implemented" — true entirely by omission, not by what the token itself permits).

Built `domains/portfolio/` (Docs/DOMAIN_OWNERSHIP.md's Portfolio domain — accounts, positions, balances, immutable snapshots) and `providers/schwab/adapter.py` (plain `httpx`, no third-party SDK). Schwab's own reference docs require an authenticated developer login and could not be fetched directly (unlike Google's, which were publicly fetchable in Phase 10) — every endpoint shape, OAuth mechanic, and JSON field name was corroborated across multiple independent real-integration sources first (CONSTITUTION.md: Provider Due Diligence), then *re-verified against the real connected account* before being trusted, which is exactly where this phase's most interesting findings came from.

**Three real, non-obvious Schwab-specific findings, verified before (or immediately after) writing code:**
1. No read-only OAuth product exists (`scope=api` is a fixed literal, not a read/write choice) — already covered above.
2. Token lifetimes are asymmetric and short: access tokens expire in 30 minutes, and — unlike Google's refresh tokens, which last until revoked — Schwab's refresh token itself has a hard 7-day expiry with no programmatic renewal. `domains/portfolio/schemas.py`'s `SchwabCredential` tracks `refresh_token_expires_at` separately, and `domains/portfolio/policies.py`'s `is_refresh_token_expired` is checked *before* ever attempting a refresh, so a doomed refresh call is never made — the caller gets a distinct `SchwabReauthorizationRequiredError` instead of a generic provider failure.
3. The registered redirect_uri (`https://127.0.0.1:8182`, fixed by the developer app's own registration) is never actually reachable — nothing listens there, on any machine. Unlike Phase 11's Calendar OAuth (where the browser being on a different machine than the backend was the blocker, solved with a Cloudflare tunnel), this isn't a network-reachability problem at all: Schwab's own documented pattern is that the user's browser lands on a dead page after consent and copies the resulting URL out of the address bar by hand. `POST /portfolio/schwab/oauth/complete` accepts that pasted URL (or just the bare code) directly — no tunnel needed, and the flow works identically regardless of which machine the browser is on.

**Two real bugs found only by live testing against the real connected account, not by unit tests — both now fixed, and both now have regression tests written *after* understanding the actual failure, not just re-asserting the fix:**

1. **Asset classification defaulted incorrectly for every ETF.** Schwab reports an ETF's `assetType` as the broad `"COLLECTIVE_INVESTMENT"` bucket — the same category a mutual fund would likely use — with the actual ETF classification living in a separate, more specific `instrument.type` field (`"EXCHANGE_TRADED_FUND"`), confirmed by inspecting the real raw response for a real held ETF (SCHF) rather than guessing. `domains/portfolio/policies.py`'s `parse_positions` now checks `instrument.type` first, falling back to the broader `assetType` map only when the specific field is absent — every real ETF position (QMOM, SCHF, IBIT, SPYM, SCHG, SCHA) had been silently misclassified as `AssetType.OTHER` before this fix.

2. **A materially more serious bug: every portfolio sync silently orphaned a fresh, duplicate set of position and balance rows.** `domains/portfolio/schemas.py`'s `Account.account_id` is a fresh random id on every `parse_account()` call (by design — the same `default_factory` pattern every other domain schema in this codebase uses). `PostgresPortfolioRepository.save_account`'s upsert-by-`account_hash` correctly updated the *existing* database row's fields in place, but — before this fix — silently discarded that existing row's real `account_id` and never returned anything, so `domains/portfolio/service.py`'s `sync()` kept using the fresh, throwaway id it had just generated for every subsequent position/balance save. The effect: `portfolio_accounts` stayed correctly deduplicated (matched by hash), but `portfolio_positions` and `portfolio_account_balances` accumulated a full duplicate set on every single sync, permanently orphaned under an account_id no account row actually had. Four manual live-testing syncs during this phase left 56 duplicate position rows (14 real positions × 4) and 8 duplicate balance rows in the real database before this was caught — cleaned up as part of the fix, not left in place. Root cause and fix: `save_account` now returns the *persisted* `Account` (with the stable, pre-existing `account_id` when one is found), and `service.py`'s `sync()` uses that returned value, never the throwaway one it parsed. Critically, **the original unit tests did not catch this** — `tests/unit/domains/portfolio/fakes.py`'s `FakePortfolioRepository.save_account` had the identical bug (silently storing whatever it was given, never modeling the real upsert-by-hash-preserving-id contract), so the fake and the real bug agreed with each other. The fake was fixed to match the real repository's actual contract, and a new regression test (`test_repeated_sync_reuses_the_same_account_and_position_rows`) asserts on the specific invariant that broke — same account_ids and no row-count growth across two consecutive syncs — not just "the bug I found is gone."

A live encoding false alarm, also worth recording precisely because it *wasn't* a bug: masked account numbers initially appeared as `â€¢â€¢â€¢â€¢1234` (mojibake) when viewed through `python -m json.tool` in this session's Windows terminal. Traced the string through every layer — the in-memory Python value, the Postgres round-trip, and the raw HTTP response bytes read directly (bypassing any terminal rendering) — and confirmed all three were correctly UTF-8-encoded `"••••1234"` throughout; the mojibake was purely how this particular terminal tool displayed the correctly-encoded bytes, not a defect in Echo. Recorded so a future session doesn't waste time re-investigating the same non-bug, and as a reminder that "verify against reality" sometimes means confirming something is *not* broken, not just catching what is.

Verification, all six PROMPT.md Phase 12 criteria, confirmed live against the real connected Schwab account (2 real accounts, 14 real positions, ~$16.2k in real holdings) — not fixtures: (1) `total_market_value` reconciled to Schwab's own `liquidationValue` within the cents-level tolerance on every sync, `reconciled: true`; (2) individual position `market_value` fields sum into that same reconciled total, verified the same way; (3) positions missing a field (e.g. a cash-equivalent fund with no `averagePrice`) keep that field `None` in both the schema and the API response, never estimated; (4) a reconciliation mismatch (tested by deliberately supplying a wrong `schwab_reported_total` in a unit test, since forcing a genuine partial failure from the real API on demand isn't practical or safe) produces a specific, visible warning string in the snapshot, not a silent pass; (5) no method anywhere in `providers/schwab/adapter.py`, `domains/portfolio/`, or `application/` places, modifies, or cancels an order — confirmed by the fact that no such method exists to call, not by a runtime check; (6) real account numbers are used only transiently inside `mask_account_number` and never appear in any persisted row, log line, or API response — confirmed by inspecting the raw wire bytes of a live `/portfolio/accounts` response and finding only the masked `••••NNNN` form.

Final state: `ruff format --check`, `ruff check`, `mypy` (0 issues across 132 source files), `check_architecture.py`, `check_size_limits.py`, `vulture`, `bandit`, `pip-audit` all clean; 264/264 tests passing (229 carried over + 35 new), 88% overall coverage. Live stack rebuilt three times across the two live-discovered bug fixes, each rebuild re-verified against the real connected account, and the orphaned duplicate rows those bugs produced during testing were cleaned from the real database as part of closing this phase out.

## 2026-07-21 — Phase 13 (portfolio calculations and dashboard) completed

Turned Phase 12's verified Schwab data into deterministic analysis, entirely inside `domains/portfolio/policies.py` (pure functions, no I/O) and a new `PortfolioService.get_dashboard()` — no new domain, no new provider, no new tables (the `computed_value_records` table already existed from Phase 4, unused until now).

**Two scoping decisions made without a code-derivable answer, recorded here rather than left implicit:**

1. **Sector mapping (PROMPT.md Phase 13 implement item 5) is real but deliberately empty of content.** Docs/DOMAIN_OWNERSHIP.md assigns sector/industry classification to the Research domain's "Fundamental Data"/"Industry Data" (Portfolio owns "Sector Allocation" as a *calculation*, not the underlying classification), and Research isn't built until PROMPT.md Phase 16. Schwab's own trader API returns no sector field on a position. Rather than fabricate a static symbol-to-sector table with no real source or provenance behind it — which the project's own "never estimate missing data" discipline (already applied to cost basis in Phase 12) forbids just as much for a whole category of data as for a single field — `compute_sector_exposure` reports a single, honest `"Unknown"` bucket covering the full portfolio, and the dashboard's `warnings` list says explicitly why. This is a real, live-verified behavior, not a stub: it correctly reflects that no real sector data source exists yet, and will need no interface change when Phase 16 makes one available (only `compute_sector_exposure`'s implementation changes).
2. **"Money dashboard" (implement item 10) means a computed read-model API (`GET /portfolio/dashboard`), not a new frontend page.** `frontend/` has no page beyond a bare `app/layout.tsx` at this point in the build, and PROMPT.md's own Phase 22 is literally "Unified dashboard" — a later, cross-domain frontend phase. Building real frontend UI now, ahead of Phase 22's actual design, would be exactly the kind of premature scaffolding CONSTITUTION.md's No Future Scaffolding Rule prohibits. `MoneyDashboard` (domain schema) / `MoneyDashboardResponse` (API schema) is the complete, real vertical slice for this phase; Phase 22 consumes it rather than duplicating its calculations.

**Calculations implemented in `domains/portfolio/policies.py`, all covered by new unit tests (PROMPT.md Phase 13 verification 2):** `compute_position_weights`, `compute_asset_class_exposure`, `compute_sector_exposure`, `compute_cross_account_exposure` (same symbol held in 2+ accounts, aggregated — symbols in only one account are correctly excluded, not "exposure" in the cross-account sense), `compute_concentration_warnings` (flags any position over a 10% default threshold — Portfolio's own business rule per DOMAIN_OWNERSHIP.md's "concentration limits", used until PROMPT.md Phase 14's Investment Policy Statement can supply a user-specific one), `compute_gain_loss` (cost basis and gain/loss stay `None`, never estimated, when Schwab never reported `average_price` — PROMPT.md Phase 13 verification 4), `is_snapshot_stale` (24-hour threshold).

**Rounding policy (verification item 3), documented once as module constants rather than scattered magic numbers:** dollar amounts round to 2 decimal places, percentages to 2 decimal places (hundredths of a percent). Money arithmetic stays `float` throughout, consistent with every other money field already in this codebase since Phase 12 (`Position.market_value`, `AccountBalance.cash_balance`, etc.) — introducing `Decimal` only for the new calculations while every existing money field stays `float` would be an inconsistent half-migration, not a real improvement; no `Decimal` usage exists anywhere else in the codebase to be consistent with.

**Calculation provenance (verification item 9)** reuses `core.provenance.ComputedValueRecord` directly rather than inventing a Portfolio-specific mechanism — `get_dashboard()` persists one record per call (`calculation_name="portfolio.money_dashboard"`, `input_record_ids` = the snapshot id plus every distinct position `source_record_id`, `rounding_policy` as a human-readable string) via the already-existing `PostgresComputedValueRecordRepository` (built in Phase 4, unused by any domain until now), and returns its id on `MoneyDashboard.computed_value_record_id` so any displayed number is traceable back to exactly what was computed and from what inputs (verification item 1: "every displayed number traces to snapshot records" — the dashboard never triggers a live Schwab call itself, only ever reads the latest already-synced, reconciled snapshot and its position rows).

**Process note:** a research-only fork dispatched to investigate this phase's data-model gaps (frontend precedent, sector data availability, provenance pattern) disregarded its "do not write code" instruction and began independently implementing the same design directly in the working tree, mid-edit, concurrently with this session's own edits — caught immediately via an unexpected "file modified since read" error on a duplicate error-class definition. The fork was stopped (`TaskStop`) before further concurrent edits could corrupt shared files. Its (correct, well-reasoned) partial work — new schemas in `domains/portfolio/schemas.py` and constants/imports in `policies.py` — was inspected, found sound, and completed by this session rather than discarded, since independent convergence on the same design was itself a useful signal. No data was lost, but this is worth remembering: a "research-only" fork sharing this session's full context can still drift into the standing "keep implementing phases" instruction, and concurrent-edit hazards from background agents are real, not theoretical.

Verified live against the real connected Schwab account (not fixtures): triggered a fresh `POST /portfolio/sync` (reconciled, $16,185.68 across 2 real accounts, matching Phase 12's known figures — the 7-day refresh token was still valid), then `GET /portfolio/dashboard`. Real, sensible output: 14 positions; asset class mix 74.61% equity / 23.95% ETF (summing to 98.54% with the remainder correctly being cash, which has no `Position` row and so isn't itself weighted); `IBIT` and `SMCI` correctly identified as genuinely held across both real accounts (`cross_account_exposure`); a real concentration warning on `UNH` at 42.92% (a real single-position concentration in this account, not a synthetic test case); `sector_exposure` correctly reporting the single `Unknown` bucket with its warning; $728.26 total unrealized gain across all 14 positions, all of which had real cost basis data (no missing-cost-basis warning fired, confirming the "stays `None`" path is dormant only when genuinely inapplicable); `is_stale: false` immediately after sync. Confirmed a request for a user with no sync produces `404 portfolio_snapshot_not_found` with the documented message, not a fallback live computation. Fetched the persisted `ComputedValueRecord` directly from real Neon by its returned id and confirmed its `calculation_name`, `rounding_policy`, `input_record_ids` (3: 1 snapshot + 2 accounts' distinct raw-response source records), and `output` all matched. Double-checked a `\xe2\x80\x94` (em dash) in a warning string that displayed as `�` in the Windows terminal by reading the raw response bytes directly — confirmed correctly UTF-8-encoded throughout, the same non-bug pattern already documented in Phase 12's mojibake investigation, not re-litigated as a new issue.

Final state: `ruff format --check`, `ruff check`, `mypy` (0 issues across 133 source files), `check_architecture.py`, `check_size_limits.py`, `vulture`, `bandit`, `pip-audit` all clean; 268/268 tests passing (232 unit + 36 integration), 86% overall coverage.

## 2026-07-21 — Phase 14 (IPS system) completed

Added a written, versioned Investment Policy Statement (IPS) and deterministic compliance evaluation entirely inside the existing `domains/portfolio/` module (schemas, errors, policies, repository, service) — no new domain (Docs/DOMAIN_OWNERSHIP.md already lists "Investment Policy Statement (IPS)" and "IPS Compliance Evaluation" under Portfolio's own "Owns"), no new provider, and no new frontend page, consistent with Phase 13's own "computed read-model API, not UI" scoping decision.

**Versioning model:** `IPSVersion` rows are immutable once written — an edit never mutates a prior version's rules, it inserts a brand-new row and flips the previously-active row's `is_active` flag off in the same call (`PostgresIPSRepository.save_version`). `ips_id` stays stable across a document's history; `version_number` increments. This satisfies PROMPT.md Phase 14 verification 3 ("updating an IPS does not rewrite historical evaluations") by construction: a `ComplianceResult` cites an exact `ips_version_id`, and that version's own rule content can never change underneath it — only which version is *active* for future evaluations changes.

**Rule evaluation** (`domains/portfolio/policies.py`'s `evaluate_compliance`) is a pure function — no I/O, no randomness (verification 1: "IPS rules are deterministic") — checking three rule types against the given positions/balances: allocation ranges (per `AssetType`, min/max % of scoped total value), a concentration rule (max % any single position may represent — the same mechanism as Phase 13's dashboard, see below), and restricted securities (exact symbol match). Rules scope to `IPSVersion.account_ids` when non-empty (PROMPT.md Phase 14 implement item 4, "account assignment"), computing a scoped total value from just that subset of accounts' positions and cash balances — otherwise every account is in scope, mirroring `build_snapshot`'s own total calculation so the two stay consistent with each other. `PortfolioService.evaluate_ips_compliance` requires both an active IPS (`NoActiveIPSError` if none) and an existing synced snapshot (`PortfolioSnapshotNotFoundError`, reused from Phase 13) — like the money dashboard, it never triggers a live Schwab call, only reads the latest already-synced data, and every `ComplianceResult` cites both the exact `ips_version_id` and `snapshot_id` it was evaluated against (verification 2).

**Phase 13's concentration threshold is now real, not just a placeholder comment.** Phase 13 shipped `_DEFAULT_CONCENTRATION_THRESHOLD_PERCENT = 10.0` with a comment promising it was "used until an Investment Policy Statement (PROMPT.md Phase 14) can supply a user-specific one." `PortfolioService.get_dashboard` now looks up the user's active IPS and, when one exists, passes its `concentration_rule.max_position_percent` into `compute_concentration_warnings` instead of the generic default — verified live (see below) by watching the dashboard's own `concentration_warnings[].threshold_percent` change from `10.0` to a real IPS-set `20.0` the moment an IPS was created, with a previously-flagged position (`SMCI` at 14.29%) correctly dropping out of the warning list once it no longer exceeded the new, higher threshold.

**Write-time validation** (`validate_ips_rules`) rejects a malformed allocation range (`min_percent > max_percent`), duplicate allocation ranges for the same asset type, and duplicate or empty restricted-security symbols, before anything is persisted — `ConcentrationRule.max_position_percent`'s own Pydantic field constraint (`gt=0, le=100`) already rules out a malformed concentration rule at the type level.

**Drift dashboard** (implement item 10) is `GET /portfolio/ips/compliance/latest` — the latest already-evaluated `ComplianceResult`, read-only, mirroring the money dashboard's own read/evaluate split (`POST /portfolio/ips/compliance/evaluate` does the work; the `GET` just reads what was last computed).

Verified live against the real connected Schwab account and its real Phase 13 data (14 positions, $16,185.68, UNH at 42.92% concentration, SMCI held across both accounts): created a real IPS (equity capped at 60%, ETF required 10-40%, 20% max single-position concentration, SMCI restricted) via `POST /portfolio/ips`, then `POST /portfolio/ips/compliance/evaluate` correctly produced 4 real breaches — an allocation-range breach (74.61% equity against the 60% cap), a concentration breach (UNH at 42.92% against the 20% limit), and two restricted-security breaches (SMCI held in both real accounts, each position flagged separately, which is correct — not a bug, since evaluation runs per-position). Confirmed `GET /portfolio/dashboard`'s concentration threshold changed live from the 10% Phase 13 default to the new IPS's 20% the moment the IPS existed. Created a second IPS version superseding the first (`POST /portfolio/ips` with the existing `ips_id`) and confirmed live against real Neon: `version_number` incremented to 2, the new version became active, and the prior version's own stored rules were completely unchanged (`max_position_percent` still `20.0`) — only its `is_active` flag flipped, exactly matching verification 3. Confirmed `404 no_active_ips` for a user with no IPS attempting to evaluate compliance. Migration reversibility tested against real Neon (`alembic downgrade -1` / `upgrade head`). All test IPS versions and compliance results created during live verification were deleted from the real database afterward (the real `schwab_credentials` and Phase 13 portfolio data were untouched) — this test content (an arbitrary threshold set, a "test restriction" reason string) wasn't the user's actual investment policy and didn't belong left in real state, the same judgment applied to Phase 12's orphaned-row cleanup.

Final state: `ruff format --check`, `ruff check`, `mypy` (0 issues across 134 source files), `check_architecture.py`, `check_size_limits.py`, `vulture`, `bandit`, `pip-audit` all clean; 288/288 tests passing (248 unit + 40 integration), 87% overall coverage.

## 2026-07-21 — Phase 15 (financial data provider evaluation harness) completed

Asked the user how to handle credentials before writing any code, since this phase fundamentally requires real accounts/API keys with external providers and the phase spec explicitly says not to trust a free-tier claim without a successful live request. The user supplied real Finnhub and Financial Modeling Prep (FMP) API keys directly; SEC EDGAR and the Senate/House Stock Watcher congressional-disclosure dumps are keyless/public and were evaluated regardless.

Built `scripts/provider_evaluation/` (`metrics.py`, `clients.py`, `runner.py`, `report.py`) and `scripts/run_provider_evaluation.py` — deliberately **not** `providers/<name>/adapter.py` or a new domain. PROMPT.md Phase 15 is explicit: "Do not select a permanent provider before this phase." No application or domain code imports anything from this package; a provider only earns a real adapter once a later phase actually adopts it. `finnhub_api_key`/`fmp_api_key`/`research_contact_email` were added to `core.config.Settings` (never read via bare `os.environ` per CONSTITUTION.md) but are used by nothing except this harness.

**Design:** every one of PROMPT.md's 13 criteria is answered honestly rather than uniformly scored. `authentication_success`, `actual_free_access`, `response_latency`, `field_completeness`, `rate_limits_observed`, `symbol_coverage`, `historical_depth`, and `data_freshness` come from an actual live request made at report-generation time (`Outcome.PASS/FAIL/PARTIAL`). `documentation_quality`, `reliability`, `licensing_constraints`, `schema_stability`, and `cost_after_free_limits` are marked `Outcome.NOT_LIVE_TESTABLE` with their source stated explicitly (a documentation/pricing-page read, or "requires monitoring over time") — PROMPT.md lists these as criteria to evaluate, but a single scripted run cannot honestly produce a live-tested score for them, and fabricating one would violate the phase's own stated purpose. A provider with no configured credential gets `Outcome.NOT_EVALUATED` per need, distinct from an attempted-and-failed request.

**A real bug found in the harness itself before trusting its output — worth recording as a caution for any tool that judges "did this call succeed?":** the first pass's `symbol_coverage` check for both Finnhub and FMP used raw truthiness of the parsed JSON body (`bool(response.json_body)`) to decide whether a symbol returned "real data." FMP's actual response to a rejected/legacy-endpoint request is a *non-empty* JSON object (`{"Error Message": "Legacy Endpoint : ..."}`), which is truthy — so a request that outright failed was being counted as a coverage "pass." Caught by manually reading the first live report rather than trusting the summary counts, not by an automated check. Fixed with a shared `response_has_real_data()` helper that requires both a populated body *and* `auth_and_access_outcomes` agreeing the request actually succeeded, and re-ran the harness — FMP's fundamentals `symbol_coverage` correctly flipped from a false `pass` to an honest `fail`. A reminder that "verify against reality" applies to a session's own tooling, not just the target system.

**Real, live-discovered findings (the actual point of this phase), from the live report at `Docs/decisions/PROVIDER_EVALUATION_2026-07-21.md`:**
- **Finnhub**: 23 pass / 3 fail / 2 partial across fundamentals, earnings, analyst ratings, and company news — real free-tier access confirmed live, with populated fields, sub-500ms latency, and real `x-ratelimit-*` response headers (60/min observed). `market_history` (the `/stock/candle` endpoint) returned a genuine `HTTP 403 "You don't have access to this resource"` — candles are not included in this free tier, a real, load-bearing finding for anyone assuming Finnhub covers "market history not already supplied by Schwab" (PROMPT.md Phase 15's need 8).
- **FMP**: 7 pass / 22 fail / 7 partial — nearly every `/api/v3/...` endpoint this key was tried against returned `HTTP 403` with `"Legacy Endpoint : Due to Legacy endpoints being no longer supported - This endpoint is only available for legacy users who have valid subscriptions prior August 31, 2025."` FMP has deprecated the v3 endpoint surface (fundamentals, earnings, analyst ratings, news, SEC filings, historical price) for this key/plan since that cutoff; the API shape recalled from general knowledge is stale. A real candidate for this need would require re-testing against FMP's current `/stable/` endpoint structure with this same key, not assumed to work from outdated documentation.
- **SEC EDGAR**: 6 pass / 0 fail / 1 partial, keyless — real Form 4 and filing-history data confirmed for AAPL (589 Form 4 filings among the 1000 most recent, spanning 2015-05-29 to 2026-06-17). No ticker-based lookup endpoint exists; a ticker→CIK map must be fetched and cross-referenced first (`sec.gov/files/company_tickers.json`).
- **Senate/House Stock Watcher** (congressional disclosures candidate): both known public S3 URLs (`senate-stock-watcher-data`, `house-stock-watcher-data` buckets) returned a real `HTTP 403 AccessDenied` — these previously-public datasets are not currently reachable at their documented URLs. PROMPT.md Phase 15's own instruction ("do not trust... unless an actual request succeeds") caught exactly this: an assumption from general knowledge that would have been silently wrong if not tested live. This need has no working live-tested source as of this report; finding one is open work for Phase 19 (congressional disclosure pipeline).
- **Reddit** (unauthenticated public JSON, company-sentiment candidate): a real `HTTP 403` on the first request — confirms Reddit blocks unauthenticated/scripted access to its `.json` endpoints even with a descriptive User-Agent, as its own terms describe. Not a viable candidate without registering for Reddit's real, paid-above-a-threshold OAuth API.
- **Polygon, Bloomberg, Reuters**: no credentials available this pass — listed explicitly in the report as known, unattempted candidates rather than silently omitted.

No permanent provider is selected by this phase, per PROMPT.md's own instruction — that decision belongs to Phase 16 (research ingestion foundation) and beyond, informed by this report.

Final state: `ruff format --check`, `ruff check`, `mypy` (0 issues across 140 source files), `check_architecture.py`, `check_size_limits.py`, `vulture`, `bandit`, `pip-audit` all clean; 307/307 tests passing (267 unit + 40 integration). `pyproject.toml`'s `[tool.setuptools.packages.find]` gained `"scripts*"` — the harness's own tests were the first to need `scripts.*` importable as a real package (previously only invoked as `python scripts/x.py`, never imported), which surfaced and required fixing this packaging gap.

## 2026-07-21 — Phase 16 (research ingestion foundation) completed

Built `domains/research/` (Docs/DOMAIN_OWNERSHIP.md's Research domain, populated for the first time — was empty since Phase 0) and adopted Finnhub and SEC EDGAR as real `providers/research/finnhub/` and `providers/research/sec_edgar/` adapters, both directly following from Phase 15's live findings (FMP was excluded — its entire v3 API surface was found deprecated for the configured key). This is the first time this codebase has ingested from **two** live providers into one shared domain schema simultaneously, and the phase's real deliverable is the generic machinery that makes that safe: entity resolution, conflict-visible field resolution, and lineage — proven end-to-end against a real, single concrete record type (issuer identity / security master) so later phases (17 news, 18 Form 4, 19 congressional) can reuse this same machinery for their own normalized record types without rebuilding it.

**Data model**: `ProviderClaim` (immutable, one per provider per sync — exactly what a provider said, never overwritten) versus `Issuer`/`SecurityMasterEntry` (the provider-independent, resolved view). `Issuer` re-resolves its own fields from *every* claim on record on every sync (`domains/research/policies.py`'s `resolve_issuer_fields`), never incrementally patched — deterministic regardless of ingestion order, verified by a dedicated unit test asserting identical output from the same two claims in reversed order.

**Entity resolution** (PROMPT.md Phase 16 implement item 7): CIK match first (SEC's identifier is authoritative and stable), ticker match as fallback for a provider (Finnhub) that supplies no CIK at all. Fixed a real correctness issue found during design, before it ever ran: an initial single-pass version resolved identity using only whichever provider's raw fetch happened to complete first in dict-iteration order, meaning a CIK from a *later* provider in the same sync would never be consulted for matching. Restructured to two passes — fetch and parse every configured provider first, *then* resolve identity preferring any CIK found across all of them — so entity resolution never depends on registration or network-response ordering, an implementation detail that shouldn't be able to silently create duplicate issuers for the same real company.

**Provider fallback rules** (implement item 10) are an explicit per-field priority table (`_FIELD_PROVIDER_PRIORITY`) rather than "whichever provider is more recent": SEC EDGAR is preferred for `name` (a real, government-verified legal name) and `cik` (the only provider that supplies one); Finnhub is preferred for `industry`, since SEC only supplies a regulatory SIC classification, not a display-oriented one. One provider failing (`ProviderUnavailableError` or any other exception) is caught per-provider and recorded as a warning without aborting the sync — the others still get ingested and resolution still succeeds off whatever data *is* available (`NoProviderDataAvailableError` only when literally everything failed).

**Verified live** against the real Finnhub and SEC EDGAR APIs (not fixtures) for AAPL — and it immediately produced exactly the kind of finding PROMPT.md Phase 16's verification criteria exist to catch:
- **Verification 1** (two providers map into the same schema): confirmed — one `Issuer` (`cik: "0000320193"`, the real Apple CIK) carrying claims from both `finnhub` and `sec_edgar`.
- **Verification 2** (source conflicts remain visible): two *real*, previously-unknown disagreements surfaced on the very first live sync — `name` ("Apple Inc" from Finnhub vs. "Apple Inc." from SEC EDGAR, a trailing-period difference) and `industry` ("Technology" from Finnhub vs. "Electronic Computers" — SEC's real SIC description, title-cased, not the all-caps guess a unit test fixture had assumed before this was checked live). Both conflicts are stored on the `Issuer` with every provider's own value intact, not silently resolved and discarded.
- **Verification 3** (provider replacement doesn't alter domain interfaces): structural by construction — `ResearchProviderPort` is one method (`get_issuer_profile`), and `application/research_provider_factory.py` is the only place that knows which concrete adapters currently back `"finnhub"`/`"sec_edgar"`.
- **Verification 4** (every normalized item retains source lineage): confirmed — the synced `Issuer.source_record_ids` held 2 entries, and fetching one directly from real Neon (`PostgresSourceRecordRepository.get`) resolved to the correct provider, origin, and raw-response reference.

Also confirmed live: re-syncing AAPL a second time reused the exact same `issuer_id` (entity resolution is idempotent — not a new issuer per sync) while claims correctly accumulated as history (2 → 4, an immutable append-only log, same pattern as `PortfolioSnapshot`); `GET /research/issuers?ticker=AAPL` and the evidence-package endpoint both round-tripped correctly; a request for a never-synced issuer_id produced `404 issuer_not_found`, not a fallback live fetch.

**`providers/research/` naming deviates from PROMPT.md Section 7's original Phase 0 sketch** (which named subdirectories by research need — `sec/`, `congressional/`, `market_data/`, `fundamentals/`, `news/` — rather than by vendor). Resolved the same way ADR_0005 resolved other Section 7 conflicts: vendor-named subdirectories (`finnhub/`, `sec_edgar/`) were used instead, matching every other populated `providers/` subtree in this codebase, since a single vendor commonly serves more than one research need (Finnhub alone covers fundamentals, earnings, analyst ratings, and company news per Phase 15's findings) and the need-first sketch would have required splitting one vendor's adapter across multiple directories.

This AAPL research data is real, correctly-computed, and not user-scoped (unlike Phase 12/14's per-user test artifacts) — left in the real database rather than cleaned up, since it's exactly the kind of genuine content this domain exists to hold.

Final state: `ruff format --check`, `ruff check`, `mypy` (0 issues across 154 source files), `check_architecture.py`, `check_size_limits.py`, `vulture`, `bandit`, `pip-audit` all clean; 336/336 tests passing (289 unit + 47 integration).

## 2026-07-22 — Phase 17 (news intelligence) completed

Asked the user how to handle the Claude synthesis step (implement item 9) before writing any code, since no live `ANTHROPIC_API_KEY` existed in this environment — Phase 7's Claude adapter had only ever been unit-tested against a mocked SDK client. The user supplied a real key. This turned out to matter: two real, load-bearing bugs were found live in code this phase touched, neither of which any prior phase's testing could have caught, since this phase's first live digest run was literally the first time Claude or Ollama's structured-output path had ever been exercised against real APIs together in one pipeline.

**Extends `domains/research/` rather than creating a new domain** — Docs/DOMAIN_OWNERSHIP.md already lists "News" under the Research domain's "Owns". New schemas: `NewsArticle` (provider-independent, enriched in place as it moves through the pipeline), `NewsDigest` (immutable, PortfolioSnapshot-style — a new digest is a new row), `NewsFeedback` (implement item 10, recorded but not yet consumed by scoring — an honest, documented gap, not a fabricated closed loop).

**Layering**: ingestion (`domains/research/service.py`'s `ingest_news`) and the deterministic policies (source quality, clustering, scoring, suppression, selection — all in `domains/research/policies.py`, pure functions, no model calls, per CONSTITUTION.md: "deterministic calculations belong in code") stay inside the domain. Classification, summarization, and synthesis all need the Model Gateway, and the portfolio-relevance boost needs a second domain — both cross-cutting concerns only the Application layer may coordinate, so `application/orchestrators/news_intelligence.py` (`NewsIntelligenceOrchestrator`) owns that sequencing, matching `memory_extraction.py`'s established precedent.

**Deduplication** (`cluster_duplicates`, implement item 4) is deterministic — Jaccard similarity over non-stopword headline tokens plus a 48-hour time window — explicitly *not* a model call, since a duplicate-or-not decision is exactly the kind CONSTITUTION.md says belongs in code. **Source quality** (implement item 2) and **event-type weight** (an input to implement item 6) are both static, explicit tables for the same reason, not model guesses.

**Event-type classification** (implement item 5) uses `generate_structured` against a closed `EventType` enum — the model cannot express a category outside the fixed vocabulary, which is what makes verification 5 ("the local model cannot silently invent facts") true by construction, not merely by prompt instruction. **Relevance scoring** (implement item 6) combines source quality × event-type weight × a recency half-life decay, with `is_portfolio_holding` (implement item 7, computed by the orchestrator from the real, synced Portfolio domain — Application-layer cross-domain query per Docs/DOMAIN_OWNERSHIP.md's Cross-Domain Interaction Matrix) applying a 1.5× boost. **Thesis matching** (also implement item 7) is *not* applied — no Investment Thesis storage exists anywhere in this codebase yet; a documented gap, not a fabricated boost, same discipline as Phase 13's sector-mapping decision.

**Two real bugs found live, both in the model-calling path, neither catchable by the existing test fakes:**

1. **Claude rejected every request with `HTTP 400 "temperature is deprecated for this model"`.** `providers/models/claude/adapter.py`'s `ClaudeAdapter.generate` forwarded `request.temperature` to the Anthropic SDK whenever it was set — every prior phase that used Claude (`ConversationOrchestrator` included) set a temperature, but none had ever run against a real key, so this was never exercised live before now. Fixed by never forwarding `temperature` to Claude at all (Ollama still honors it) — `ModelRequest` keeps the field so callers don't need to know which provider they're targeting.
2. **Every article's `summary` silently fell back to its own headline.** `_SUMMARIZE_PROMPT` asked the model to "Reply with ONLY the summary, nothing else" — plain text — but `_summarize` parsed the response via `generate_structured` against `_SummaryOutput` (`{"summary": "..."}`), which requires JSON. `ModelGateway.generate_structured`'s JSON extraction found no `{`/`}` in the plain-text reply, raised `ModelOutputInvalidError`, and the orchestrator's own fail-safe (`except EchoError: return article.headline`) silently absorbed it — exactly working as designed, just triggered by a prompt bug instead of a genuine model failure. Fixed by asking for the JSON shape explicitly, matching the classification prompt's own style.

Both bugs are a direct instance of a lesson already recorded twice this session (Phase 12, Phase 15): **`FakeNewsModelGateway`'s `generate_structured` returns a pre-built Pydantic object directly from a queue — it never exercises the real JSON-text-extraction-then-validation path `ModelGateway` actually does.** The fake and the bug agreed with each other, so all 61 orchestrator/policy unit tests passed while the real pipeline silently degraded to headline-only summaries and threw away every Claude call. Live verification — required per this project's own standing discipline — is what caught both, not the otherwise-clean test suite.

**Verified live** against the real connected Schwab portfolio (Phase 12/13), Finnhub, SEC EDGAR, Ollama, and Claude — not fixtures. Synced a real `Issuer` for UNH (UnitedHealth Group, the account's largest real holding at 42.92%) via the real SEC EDGAR/Finnhub pipeline (real CIK `0000731766`, a real live-discovered conflict on `industry`: Finnhub's "Health Care" vs. SEC's SIC "Hospital & Medical Service Plans"). Ran the full news digest pipeline end to end:
- **Verification 1** (duplicate stories collapse): 489 real articles ingested, 459 distinct clusters — 30 genuine near-duplicates correctly collapsed, including four different analysts' "Maintains [Rating] on UnitedHealth Group, Raises Price Target" notes (JPMorgan, Morgan Stanley, Oppenheimer, Truist) landing in the same cluster.
- **Verification 2** (low relevance suppressed): 69 of 489 articles fell below the 0.15 relevance threshold and were excluded before the top-5 selection ever ran.
- **Verification 3** (material portfolio news outranks generic popularity): proven end to end by a dedicated orchestrator unit test holding source, event type, and headline identical between a held and a non-held ticker — the only variable was the boost, and it was the deciding factor. UNH's own real relevance scores reflect the applied 1.5× boost throughout this live run.
- **Verification 4** (the final narrative links back to evidence): Claude's real synthesis — "UnitedHealth shares have surged after the company raised its guidance, prompting speculation about whether the stock could climb as high as $500 [2][3]. That optimism was reinforced by an upgrade to a Zacks Rank #1 (Strong Buy) ... [4]. Still, on the earnings call analysts pressed management ... [3]." — cites specific numbered articles for every claim, checked against the real numbered list it was given, not just instructed to in the prompt.
- **Verification 5** (the local model cannot silently invent facts): every live classification returned a valid `EventType` value (`earnings`, `guidance`) — structurally incapable of anything else; summaries were genuine extractive rewrites of the real blurb text once the JSON-format bug above was fixed, confirmed by inspecting several against their source blurbs.

This real UNH news/digest data (like Phase 16's AAPL issuer) is genuine research content, not arbitrary test configuration — left in the database rather than cleaned up.

Final state: `ruff format --check`, `ruff check`, `mypy` (0 issues across 156 source files), `check_architecture.py`, `check_size_limits.py`, `vulture`, `bandit`, `pip-audit` all clean; 359/359 tests passing (309 unit + 50 integration), 84% overall coverage.

## 2026-07-22 — Phase 18 (SEC Form 4 / insider transaction intelligence) completed

**Extends `domains/research/` rather than creating a new domain** — Docs/DOMAIN_OWNERSHIP.md explicitly lists "Insider Transaction Data", "insider transaction normalization", and "Trading Anomaly Detection Results" under Research's ownership. New schemas: `InsiderTransaction` (immutable per-filing record, enriched in place with `filing_context` after Ollama classification — the same enrich-in-place pattern as `NewsArticle`), `InsiderProfile` and `AnomalyFeature` (computed fresh on every read, never persisted — the same computed-on-read discipline as `MoneyDashboard`/`InsiderProfile`-style aggregates elsewhere in this codebase), `InsiderEvidenceView` (the read-time bundle).

**Ground-truth verification before writing the parser, not after.** The research fork consulted for this phase flagged the Rule 10b5-1 trading-plan indicator's exact field name as low-confidence recalled knowledge and explicitly recommended verifying against a real filed document before writing any parsing code. Heeded literally: performed live diagnostic HTTP fetches against several real UnitedHealth Group (CIK `0000731766`) Form 4 filings before writing `providers/research/sec_edgar/adapter.py`'s `_parse_form4_xml`, confirming two real, load-bearing facts that would have broken the parser if assumed from memory instead:
1. `submissions.json`'s `primaryDocument` field for a Form 4 points at an XSLT-rendered HTML view, not the machine-readable XML — the real XML filename is only discoverable via a second `index.json` fetch per accession (`get_form4_document` does this two-step retrieval).
2. The 10b5-1 indicator (`<aff10b5One>0|1</aff10b5One>`) is a **document-level**, not per-transaction, XML element — confirmed by inspecting a real director grant filing (accession `0000731766-26-000189`) and a real CEO-of-Optum open-market sale filing (accession `0000731766-26-000123`, Patrick Hugh Conway, 800 shares at $355/share, `aff10b5One=0`).

**Documented scope limitations** (smallest complete vertical slice, not silent gaps): `derivativeTable` (options/derivatives) is deliberately not parsed at all — only the non-derivative `nonDerivativeTable` transactions implement items 1-9 need. A filing with more than one `reportingOwner` (joint filers) uses only the first; a documented simplification, not a crash or a silent drop of the filing.

**Layering**: deterministic parsing/normalization (`domains/research/policies.py`'s `parse_form4_transactions`, `normalize_transaction_type`, `compute_transaction_value`, `compute_ownership_change_percent`, `compute_size_anomaly`, `compute_insider_cluster_feature`, `compute_insider_profile` — all pure functions, no model calls) stays inside the domain, matching Phase 17's own layering lesson. Footnote classification into `FilingContext` (implement item 8) needs the Model Gateway, and the opt-in Claude interpretation (implement item 10) is the one step meant to produce prose a user reads — both cross-cutting concerns, so `application/orchestrators/insider_intelligence.py` (`InsiderIntelligenceOrchestrator`) owns that sequencing.

**Anomaly detection is "personal baseline" only** (DOMAIN_OWNERSHIP.md's own phrase) — `compute_size_anomaly` compares a transaction only against the *same insider's own* prior transaction history at the same issuer (never a market-wide or peer benchmark this phase has no real data for), and requires at least 2 prior transactions to produce a feature at all; with fewer, no feature is fabricated. `compute_insider_cluster_feature` counts distinct insiders transacting in the same direction within an explicit 7-day window, stated in its own `baseline_description`. Both anomaly features are computed only for an insider's *most recent* transaction (`get_insider_evidence`), not a full historical replay per transaction — the real product question is "is this insider's latest activity anomalous," not a retrospective audit.

**Claude interpretation is explicitly opt-in** (implement item 10) — a separate `interpret()` method and a separate `POST .../interpret` endpoint, never called as a side effect of ingestion or the evidence read. The prompt directly applies CONSTITUTION.md's Verified Truth section ("Echo must distinguish between: Verified facts. Inferred conclusions. ... Every unsupported statement shall be identified as unsupported") and Language Model Constraints ("Language models may explain results. They may not define results."): Claude is given VERIFIED FACTS (real transaction data) and COMPUTED FEATURES (anomaly features with their own stated baselines) as two separate labeled sections, and is explicitly told it may not use "suspicious", "illegal", "insider trading", "fraud", or similar accusatory language, and may not assert a conclusion the given facts/features don't directly support (implement item 10's own verification 5: "Echo avoids unsupported accusations"). A dedicated orchestrator unit test asserts the guardrail text is actually present in what's sent to the model, not just claimed in a docstring.

**A real idempotency bug found live, not by the test suite.** `InsiderTransaction.transaction_id` originally used its schema default (`new_id("insidertxn")`, a random UUID per construction). Live-verifying against real UNH filings — re-running ingestion twice with overlapping `limit` values to simulate a scheduled re-sync — produced 58 stored rows from what should have been 42: `get_form4_filings` always returns the most recent N filings regardless of what's already been ingested, so any repeat ingestion re-parses already-seen filings, and a random `transaction_id` meant `save_insider_transactions`'s upsert-by-`transaction_id` logic (correct in design, same enrich-in-place pattern as `save_articles`) never actually matched the existing row. Duplicated history would have silently corrupted `compute_insider_profile`'s totals and `compute_size_anomaly`'s baselines on every re-sync. Fixed by deriving `transaction_id` deterministically from `(accession_number, index-within-filing)` in `parse_form4_transactions` — a filing's own transaction ordering is stable across re-fetches, and SEC provides no other per-transaction identifier. Re-verified live: two full ingestion passes against the same real UNH filings produced 42 stored rows both times. Regression tests added at both the policy level (`test_parse_form4_transactions_is_idempotent_across_reingestion`) and the service level (`test_reingesting_the_same_filing_does_not_duplicate_stored_transactions`).

**Verified live** against the real SEC EDGAR API (no fixtures) for UNH (CIK `0000731766`, the account's largest real holding — same issuer as Phase 17):
- **Verification 1** (grants distinguished from open-market purchases/sales): real data included both — six-plus real director/officer quarterly stock grants (`transaction_code="A"`, `$0` reported price — a real, not-fabricated $0, since a grant genuinely has no market price) and one real open-market sale by Patrick Hugh Conway (CEO of Optum), 800 shares at $355.00/share.
- **Verification 2** (planned sales identified only when data supports it): Conway's real sale filing reports `aff10b5One=0`, and `is_planned_sale` correctly resolved to `False` — never inferred from anything but that field.
- **Verification 3** (transaction values and ownership changes computed in code): the real sale's `transaction_value` = `800 × 355.00 = 284000.0`, computed, not filed directly; `ownership_change_percent` derived from the filing's own post-transaction share balance for every transaction with one reported.
- **Verification 4** (anomaly claims explain their comparison baseline): a real insider (Paul R. Garcia, 9 real recorded grants) produced a live `transaction_size_vs_personal_baseline` feature — "this transaction is 1.42x the insider's own average transaction size (93.0 shares) across their 4 prior recorded transactions for this issuer" — and a live `insider_cluster_timing` feature — "8 distinct insider(s) at this issuer recorded a acquisition transaction within 7 days of this one" — both stating their exact comparison set, never a bare score.
- **Verification 5** (Echo avoids unsupported accusations): the real Claude interpretation for that same evidence explicitly framed the cluster feature as "timing overlap across insiders (such as might occur with scheduled award dates) rather than any coordinated or unusual behavior" and closed with "the facts and features simply describe transaction sizes and timing relative to stated baselines, without indicating anything about the legality or propriety of the activity" — checked programmatically for the presence of "suspicious", "illegal", "insider trading", "fraud": zero hits.

Live-verification data (issuer `issuer_live_unh`'s insider transactions) was deleted from Neon after verification, unlike Phase 16/17's issuer/news data — Form 4 data changes SEC's own historical record only by new filings, not by re-sync, so there was no ongoing value in leaving synthetic verification-run artifacts (produced across three separate live-verification passes, including the pre-fix duplicate-producing runs) in the database the way a standing issuer profile has.

Final state: `ruff format --check`, `ruff check`, `mypy` (0 issues across 158 source files), `check_architecture.py`, `check_size_limits.py`, `vulture`, `bandit` (0 issues after adding `# nosec B405` to the SEC-trusted `xml.etree.ElementTree` import, matching the existing `# nosec B314` on the `ET.fromstring` call), `pip-audit` all clean; 393/393 tests passing (340 unit + 53 integration).

## 2026-07-22 — Phase 19 (Congressional disclosure pipeline) completed

**Real data source discovery, live, before any design was committed to.** Two long-standing free third-party aggregator APIs this session initially assumed would exist (`senate-stock-watcher-data`/`house-stock-watcher-data`, both hosted on S3) were live-checked first and found to return `403 AccessDenied` — those community projects are dead. Investigated the real official sources directly instead: the House Clerk's disclosure system still publishes Periodic Transaction Reports (PTRs) as scanned PDFs/images only (no structured data at all — a hard, real constraint, not a design choice). The Senate's own eFD system (`efdsearch.senate.gov`), by contrast, renders every **electronically-filed** PTR as a real, structured HTML transactions table — confirmed live by walking an actual senator's real filing (Alan Armstrong, 703 real transactions) end to end before writing any parser. **Scope decision**: this phase supports the Senate only; the House's PDF-only system is a documented, real limitation (`domains/research/schemas.py`'s `Chamber` docstring), not a silent gap — `Chamber.HOUSE` exists as a value (a real politician's own term history can include House service) but nothing in this phase's pipeline populates it. Some senators still file PTRs on paper even in the Senate's own system (`report_kind="paper"`) — also skipped explicitly, not silently.

**No third free API existed for politician identity or committee assignments either** — congress.gov and efdsearch.senate.gov both lack one. Found and live-verified `unitedstates/congress-legislators` (a real, actively-maintained public GitHub project) as the reference dataset: `legislators-current.yaml` (bioguide ids, and genuinely date-ranged `terms` with state/party), `committees-current.yaml` (real committee jurisdiction text), `committee-membership-current.yaml` (a *current-snapshot* membership list, not a time series). This last fact is load-bearing for verification 2: since the dataset has no per-date committee history, `providers/research/congress_legislators/adapter.py`'s own module docstring and `domains/research/schemas.py`'s `Chamber`/`CommitteeAssignment` docstrings document the honest scope this forces — committee-relationship features are computed from the current snapshot only, appropriate because committee assignments are made once per two-year Congress and stay stable within it, and are never presented as reflecting committee membership from an earlier Congress.

**Extends `domains/research/` rather than creating a new domain** — Docs/DOMAIN_OWNERSHIP.md explicitly lists "Congressional Trading Disclosures", "congressional disclosure normalization", "Committee Correlation Analysis", and "Personal Baseline Trading Profiles" under Research's ownership. New schemas: `PoliticianTransaction` (one PTR line item), `CommitteeAssignment` (a snapshot membership row), `PoliticianTradeProfile` and `AnomalyFeature` (computed fresh on every read, the same pattern as Phase 18's `InsiderProfile`), `PoliticianEvidenceView` (the read-time bundle).

**No Model Gateway anywhere in this phase** — unlike Phase 17/18, none of PROMPT.md Phase 19's ten implement items call for a classification or interpretation step; every one of them (ingestion, identity, committee history, asset identity, transaction ranges, filing delay, sector classification, historical profiles, committee relationship features, anomaly candidates) is a deterministic computation or a lookup against already-integrated data. Sector classification (implement item 7) deliberately reuses this same domain's own `sync_issuer`/`get_issuer_by_ticker` (Phase 16) rather than a new provider or a model call — `get_politician_evidence` never triggers a live sync as a side effect of a read; an unresolved ticker just means no committee-relationship feature is computed for it. No orchestrator file was added — everything lives in `domains/research/service.py` directly, since there's no second domain and no Model Gateway to coordinate (CONSTITUTION.md reserves the Application layer for exactly that coordination, which this phase genuinely doesn't need).

**Verification 1 ("transaction ranges are not converted into false exact amounts") shaped nearly every numeric design choice this phase.** `PoliticianTransaction.range_low`/`range_high` store the filing's own two disclosed boundary figures — `range_high` is `None` for a genuinely open-ended "Over $X" disclosure, never a fabricated ceiling. `PoliticianTradeProfile`'s purchase/sale totals sum each side's low and high bounds *independently*, producing a real range aggregate rather than an average masquerading as a total. The anomaly feature comparing transaction size against personal history (`compute_bracket_size_anomaly`) compares disclosed range *floors* only, and its `baseline_description` says so explicitly ("a comparison of disclosed bracket floors, never an exact transaction amount") — verified live: a real $5,000,001–$25,000,000 Williams Companies sale by Senator Armstrong was flagged as 1003.52x his own $4,982 average floor across 702 prior transactions, entirely without ever stating or implying a specific dollar value for either side of the comparison.

**Verification 2 ("committee membership uses the membership effective at the relevant date") is satisfied honestly, not by pretending to more precision than the source data supports** — see the congress-legislators discovery note above. Politician identity itself (implement item 2) *is* genuinely date-effective: `resolve_politician_identity` selects the specific `terms` entry active at the report's filing date, live-confirmed by resolving "Alan Armstrong" (the Senate eFD system's own, real, inconsistently-formatted name fields) to bioguide id `A000383`, state `OK`, party `Republican` — the actual current term's own real values, not a static guess.

**Verification 3 ("correlation is not described as proof of misconduct") and verification 4 ("every relationship can be inspected")** are both enforced in `compute_committee_relationship_features`: a deterministic keyword overlap between the traded issuer's real industry classification (reused from Phase 16, never a model judgment) and each committee's own real, published jurisdiction text. Every returned feature names the exact committee, quotes its real jurisdiction text, and lists the matched term(s) — nothing is reduced to a bare score — and its `baseline_description` explicitly states this is "a keyword overlap, not a claim about this politician's actual influence, involvement, or intent." A dedicated unit test asserts the real generated text contains none of "suspicious"/"illegal"/"insider trading"/"fraud"/"misconduct"/"proof".

**Applied Phase 18's idempotency lesson proactively instead of rediscovering it live.** `parse_ptr_transactions` derives `transaction_id` deterministically from `(report_id, index-within-filing)` from the start, rather than the schema's own random default — the exact fix Phase 18 needed only after live-verification caught duplicate rows. Still re-verified live here regardless (never assume a lesson generalizes without checking): ingested the same real Senate filings twice through the full service pipeline and confirmed the stored row count stayed identical.

**Verified live** against the real Senate eFD system and the real congress-legislators reference dataset (no fixtures) for a real, currently-serving senator (Alan Armstrong, Oklahoma): 737 real PTR transactions ingested across several real filings, including a real mix of `purchase`/`sale_full`/`sale_partial` transaction types and tickers with no reported ticker at all (a non-equity asset, left `None` rather than guessed). Real `filing_delay_days` values of 116 and 113 days were computed for two real transactions — both genuinely exceeding the STOCK Act's 45-day statutory deadline, a real, non-trivial finding `is_filing_late` correctly flags. Committee assignment sync resolved two real committees (Health, Education, Labor, and Pensions; Indian Affairs) for this senator from the live reference dataset.

Live-verification data (Senator Armstrong's 737 politician-transaction rows and 2 committee-assignment rows) was deleted from Neon after verification, matching Phase 18's own precedent for this same reason (a static historical record a re-sync doesn't change, unlike a standing issuer profile) — the real UHS issuer sync performed alongside it, however, was left in place, matching Phase 16/17's precedent for genuine issuer research data.

Final state: `ruff format --check`, `ruff check`, `mypy` (0 issues across 163 source files), `check_architecture.py`, `check_size_limits.py`, `vulture`, `bandit`, `pip-audit` all clean; 435/435 tests passing (376 unit + 59 integration). New dependencies: `pyyaml` (parses the congress-legislators dataset's YAML format) and `beautifulsoup4` (parses the Senate eFD system's real transaction table HTML — a proper parser rather than regex-scraping government HTML, the same reasoning Phase 18 used for choosing stdlib `xml.etree` over regex for Form 4 XML).

## 2026-07-22 — Phases 20-21 (Gmail read integration, Gmail approval-gated writes) deferred

User explicitly requested skipping Gmail integration. Phase 21 (approval-gated writes) is directly gated on Phase 20 (read integration) existing first, so both are deferred together, not just Phase 20. Confirmed via AskUserQuestion: this is a standing deferral, not a one-session skip — Gmail integration is not considered part of the build until the user revisits it explicitly. Proceeding directly to Phase 22 (Unified dashboard), which does not depend on the Email domain existing.

## 2026-07-22 — Phase 22 (unified dashboard) completed

**The first real frontend beyond Phase 8's minimal chat page.** Phases 0-19 built substantial real backend capability (Portfolio, Calendar, Approvals, Conversation, Research) with almost no way to see it all at once — this phase is the first cohesive view (PROMPT.md's own objective), replacing `app/page.tsx`'s placeholder status page (which had explicitly said "the unified dashboard ... is Phase 22; this status page is just a landing point until then").

**Cross-domain aggregation lives in a new `application/queries/` — the first population of that Application-layer subdirectory (No Future Scaffolding: it stayed empty until this phase genuinely needed it).** `application/queries/dashboard_query.py`'s `DashboardQueryService` coordinates Portfolio, Calendar, Approvals, and Conversation in one read, matching CONSTITUTION.md's own rule that only the Application layer may coordinate more than one domain at once — the same reasoning `NewsIntelligenceOrchestrator` and `CalendarWriteOrchestrator` already established, applied here to a *query* rather than a *write workflow* (a query is real Application-layer work, not automatically an orchestrator, hence the new subdirectory name rather than folding it into `orchestrators/`).

**Backend additions needed before the dashboard could be built at all** — three domains were each missing one small, genuinely-needed read capability no prior phase had required: `ApprovalProposalRepository.list_pending_for_user` (no "list" capability existed at all before this phase — only get-by-id), `ConversationRepository.list_recent_sessions_for_user`, and `CalendarService.is_connected`/`PortfolioService.is_connected` (both derived from the credential repository's existing `get_for_user`, never a live provider health check). "Pending" for the approval inbox specifically means `AWAITING_APPROVAL` — a proposal `ApprovalService.propose` creates in `VALIDATED` first, one step earlier in the state machine, confirmed by re-reading `VALID_TRANSITIONS` before writing the query (a `VALIDATED` proposal cannot even be approved directly, so surfacing it in an inbox as if it were actionable would have been a real, if subtle, bug).

**Every card carries its own `CardMeta` (`status` + `as_of`)** (PROMPT.md Phase 22 verification 2: "every card shows freshness or status") — a disconnected integration (no Schwab/Calendar credential) or a connected-but-never-synced one (`no_data`) are both honest, distinct states, never a hard failure that takes down the other six cards; each domain error (`CalendarCredentialNotFoundError`, `PortfolioSnapshotNotFoundError`, ...) is caught per-card inside `DashboardQueryService`, not by the API route or the frontend.

**"Projects" (implement item 4) is honestly `not_available`** — no Projects domain exists yet (that's Phase 23); a static, explicit placeholder rather than a fabricated empty-but-ok card, the same "missing stays missing" discipline used throughout every domain in this codebase.

**"Attention" (implement item 3) is two concrete, already-computed real signals** — pending-approval count and IPS compliance breaches (Phase 14) — not a speculative catch-all. Research-domain anomaly candidates (Phases 18-19) are not yet correlated against portfolio holdings here; a real, documented gap for a future phase, not a fabricated signal invented to make the card feel fuller.

**"Integration status" (implement item 6) is a credential/config-presence check, never a live health call to seven external providers on every dashboard load** — that cost would scale with every render, not with real data change. Google Calendar/Schwab check real per-user OAuth credential presence; Finnhub/SEC EDGAR-Senate eFD/Claude check real configured API keys; congress-legislators is reported connected unconditionally since it is genuinely keyless.

**Frontend**: `app/page.tsx` (replacing the Phase 8 placeholder) and a new `app/globals.css` — plain CSS, no framework dependency added for one page, matching `app/chat/page.tsx`'s own established minimal-dependency precedent. Responsive design (implement item 9): CSS grid (`auto-fit, minmax(300px, 1fr)`) collapses to one column under 640px, live-verified with a real 375px-viewport screenshot. Accessibility (implement item 10): semantic `<section>`/`<h2>` per card, visible focus rings on every interactive element, `aria-live="polite"` freshness regions, `role="group"` around approval actions. Approval actions cannot be mistaken for ordinary buttons (verification 3): distinct green/red bordered buttons with explicit `aria-label`s naming the action and the real proposal summary, plus a native `window.confirm()` step restating that same summary before either POST fires — live-verified end to end with a real pending proposal: clicked Reject, confirmed the dialog's exact text, watched the real `/approvals/{id}/reject` call empty the inbox and the Attention card update to match, zero browser console errors.

**Verified live** against the real backend (no fixtures): `GET /dashboard` for `live_user` (the real Schwab/Calendar-connected account from Phases 12-13) returned real `total_market_value` ($16,185.68), real `is_stale=false`, and real Calendar/Schwab/Finnhub/SEC EDGAR/Senate eFD/Claude/Ollama connection status in one response. Proposed a real calendar-event approval for `live_user` through the existing `/calendar/events` endpoint, confirmed it appeared in the dashboard's real approval inbox and attention card, then rejected it through the real `/approvals/{id}/reject` endpoint (never executed against real Google Calendar) and confirmed the dashboard reflected the empty inbox again. The frontend's own Docker image had to be rebuilt and the container recreated (no bind-mounted dev server — `frontend/Dockerfile` is a production multi-stage build, same gotcha this session already learned for the backend, now confirmed to apply to the frontend container too) before the browser-driven Playwright screenshots reflected the new page rather than the old placeholder.

Final state: `ruff format --check`, `ruff check`, `mypy` (0 issues across 167 source files), `check_architecture.py`, `check_size_limits.py`, `vulture`, `bandit`, `pip-audit` all clean on the backend; `tsc --noEmit`, `next lint`, and `next build` all clean on the frontend. 454/454 backend tests passing (393 unit + 61 integration). No new backend dependencies; no new frontend dependencies (plain CSS, no UI library).

## 2026-07-22 — Phase 23 (project and task intelligence) completed

**A new domain with no external providers at all** — the first phase since Phase 0 where `domains/*/service.py` never constructs a provider port. Docs/DOMAIN_OWNERSHIP.md says so explicitly for Projects ("External Providers: None — third-party project management systems are providers rather than owners"), and the implementation follows through on that literally: `domains/projects/service.py` has no `*ProviderPort` Protocol, and `application/` gained no new provider-factory function for it, unlike every other domain built so far.

**Seven real entities, one table each** (`domains/projects/repository.py`): `Project`, `Goal`, `Milestone`, `Task`, `Decision`, `Blocker`, `StatusUpdate` — matching PROMPT.md Phase 23's own implement item list one-for-one (items 1-3, 5-7), plus `document_links` (item 8) as a plain JSONB list column on `Project` itself rather than its own table, since it's just a list of URL references Projects stores but never fetches or interprets.

**`TaskStatus` gets a real, code-enforced state machine** (`domains/projects/models.py`'s `TASK_VALID_TRANSITIONS`, mirroring `domains/approvals/models.py`'s own `VALID_TRANSITIONS` precedent) — PROMPT.md verification 3 ("the assistant distinguishes proposed tasks from committed tasks") is not just a display convention here: every task is created `PROPOSED`, and `IN_PROGRESS`/`DONE` are structurally unreachable without first passing through an explicit `COMMITTED` transition. Live-verified: attempting to start a still-`PROPOSED` task through the real API returned a real `409 invalid_task_transition`, and only succeeded after the real `/commit` call.

**Decisions and Status Updates are immutable, append-only records** — the same `PortfolioSnapshot`/`NewsDigest` pattern used everywhere else in this codebase a correction is a new row, never an edit, which is what makes verification 2 ("decisions are historically traceable") true structurally. `ProjectStatusSummary` (implement item 1) is computed fresh from these stored facts on every read (`domains/projects/policies.py`'s `compute_project_status_summary`) — task/blocker counts, the next and any overdue milestones, the latest decision and status update — never a free-text summary an assistant could invent (verification 1).

**Memory integration (implement item 9) lives in a new Application-layer orchestrator**, `application/orchestrators/project_memory.py`, not inside the Projects domain itself — no direct "Projects -> Memory" row exists in Docs/DOMAIN_OWNERSHIP.md's Cross-Domain Interaction Matrix, but the matrix's general rule ("Domains SHALL interact only through: Application orchestration...") and the one directly analogous listed row ("Conversation -> Memory: Application Command") both point to the same mechanism already established by `NewsIntelligenceOrchestrator`/`CalendarWriteOrchestrator`. Only `Decision`s are linked into Memory, not `StatusUpdate`s — a narrower, real scope choice (a decision is the kind of fact CONSTITUTION.md's own Product Mission names directly: "historical decisions"). The created memory is always `MemoryStatus.CANDIDATE`, never auto-confirmed — the same discipline every other memory-producing pathway in this codebase already follows (Phase 9's own "extracted candidates are not automatically treated as confirmed facts"). Live-verified: recording a real decision through `POST /projects/{id}/decisions` returned a real `memory_id`, and `GET /memory?user_id=` confirmed a real `candidate`-status record existed with the correct `source_type="project_decision"`/`source_id` linkage back to the decision.

**Verification 4 ("task modifications use approval if they affect an external system") is satisfied by scope, not by a new approval flow** — this phase builds no Projects-to-external-system write path at all (no Calendar/Email integration; Docs/DOMAIN_OWNERSHIP.md's own matrix restricts "Projects -> Calendar" to "Application Command" for exactly this reason, a future phase's concern). Documented here explicitly as a real scope boundary, the same discipline used for Phase 19's verification 2 and Phase 22's Attention card: a verification that currently holds vacuously, not one quietly ignored.

**Dashboard summary (implement item 10) replaces Phase 22's honest `not_available` placeholder** with real data — `application/queries/dashboard_query.py`'s `DashboardQueryService` gained a fifth coordinated domain (Projects), and its Projects card now lists every non-archived project with real task/blocker counts from `get_project_status_summary`. The frontend's `ProjectsCard` component (`frontend/app/page.tsx`) was updated to render this list rather than the old static placeholder text — live-verified with a real Playwright screenshot showing "Kitchen remodel — 0/1 tasks done" with a real "1 blocker" badge, replacing what had been static text one phase ago.

**No event publishing** — `ProjectCreated`/`MilestoneCompleted`/etc. are documented as published events in Docs/DOMAIN_OWNERSHIP.md, but (matching every other domain built in Phases 16-22) no event bus infrastructure exists yet in this codebase; publishing is deferred until a phase actually needs a subscriber, not built speculatively ahead of one.

**Verified live** against the real backend (no fixtures), for both a fresh test user and the real, previously-connected `live_user` account: created a real project, added a real goal/milestone/task, walked the task through its full real state machine (`PROPOSED` → rejected-at-`IN_PROGRESS` → `COMMITTED` → `IN_PROGRESS`), recorded a real decision (confirming the real linked memory candidate), raised a real blocker, and read back a real status summary reflecting exactly those stored facts. Cleaned up all live-verification data (projects, sub-records, and the linked memory candidate) from Neon afterward, matching Phase 18/19's precedent for throwaway verification artifacts.

Final state: `ruff format --check`, `ruff check`, `mypy` (0 issues across 178 source files), `check_architecture.py`, `check_size_limits.py`, `vulture`, `bandit`, `pip-audit` all clean on the backend; `tsc --noEmit`, `next lint`, `next build` all clean on the frontend. 494/494 backend tests passing (424 unit + 70 integration). No new dependencies, backend or frontend.

## 2026-07-22 — Phase 24 (proactive monitoring foundation) completed

**The System domain is populated in code for the first time.** Phases 0-23 (minus the still-deferred Phases 20-21, Gmail integration) left `domains/system/` an empty placeholder; Docs/DOMAIN_OWNERSHIP.md already named it the owner of "platform monitoring", "system alert generation", and "raise alert"/"acknowledge alert", so this phase's monitor/alert business logic lives there — not in a new "Monitoring" domain (none exists in the 13-domain catalog) and not in Notifications (which explicitly "does not decide business meaning," only owns delivery channels/templates/retry policies, and remains unpopulated in code).

**"Notification preferences" (implement item 6) is two integer fields, not a Notifications-domain build-out** — `MonitorDefinition.quiet_hours_start_utc`/`quiet_hours_end_utc` (plain UTC hour-of-day, 0-23, `Integer` columns) live directly on the monitor. A full multi-channel delivery system was never in scope for this phase; PROMPT.md's own implement list separates "quiet hours" (item 5) from "notification preferences" (item 6) but neither requires more than this to be real and complete for a single delivery channel (the dashboard's own Alerts view).

**A real, enforced architecture rule reshaped the calendar-conflict monitor's design before any code was written.** `scripts/check_architecture.py`'s `_check_domain_isolation()` flags any domain importing another domain's schemas directly ("no-domain-to-domain-imports") — not just a style convention, a build failure. `domains/system/policies.py`'s `find_calendar_conflicts` therefore takes `list[tuple[str, datetime, datetime]]` (plain primitives), never `domains.calendar.schemas.CalendarEvent`; `MonitoringOrchestrator` (the Application layer) is the only place that imports both domains' schemas, translating typed `CalendarEvent`s into primitives before calling System's policy function — the same "provider speaks in primitives" pattern this codebase already applies one layer down. Verified with `python scripts/check_architecture.py` → "No architecture violations found."

**Verification 1 ("monitors may notify but cannot execute consequential actions") is satisfied structurally, not by convention** — `MonitoringOrchestrator.__init__` takes `SystemService`, `PortfolioService`, `CalendarService`, `ResearchService`, an audit repository, and a clock; it has no dependency on `ApprovalService` or any write-adapter/orchestrator at all, so it is architecturally incapable of executing a consequential action, the same "can't get there from here" guarantee this codebase already relies on elsewhere rather than a runtime check that could be forgotten.

**Deduplication (implement item 4 / verification 2) is enforced inside `SystemService.raise_alert`, not left to each evaluator to remember** — every monitor type has its own pure `*_dedup_key()` builder in `domains/system/policies.py` (deterministic, no side effects), and `raise_alert` checks `get_active_alert_by_dedup_key()` first, returning `None` — not raising an error — when an active duplicate already exists. Deduplication is treated as a routine, expected sweep outcome, not a failure path. Live-verified: running `/monitors/evaluate` twice in a row against the same unresolved integration failure produced exactly one alert, not two.

**Every alert carries a real `reason: str` field (verification 3), and the calendar-conflict monitor's reason names the actual meetings, not internal IDs.** The first implementation used `CalendarEvent.event_id` (an internal `calevent_...` identifier) in the alert message/reason — technically satisfying "shows why it triggered" but useless to a person reading it. Caught during test-writing (a test asserting on the literal event summary failed against the ID-based message) and fixed: `MonitoringOrchestrator._evaluate_calendar_conflict` now builds an `event_id -> summary` lookup from the same fetched events and reports `"Meeting A" and "Meeting B" have overlapping time ranges...`, keeping the internal ID only as the dedup key's input (stable identity) rather than the human-facing text.

**The "integration failure" monitor (implement examples item 6) is built on audit actions every OAuth integration already recorded, not a new failure-tracking mechanism** — `calendar.token_refresh_failed` (`domains/calendar/service.py`, Phase 10/11) and `schwab.token_refresh_failed` (`domains/portfolio/service.py`, Phase 12) were both already recorded with `result="failure"` before this phase existed. The only new capability needed was a query: `AuditRepository.list_recent_by_action(action, since, *, result=None)`, added to both the Protocol and `PostgresAuditRepository` (`infrastructure/database/repositories/audit.py`) — no existing method could filter by action/recency, only `record`/`get`/`list_for_correlation`. This monitor is honestly platform-wide, not per-user (the audit table has no `user_id` column and no Identity domain exists) — documented as an honest reflection of this being a single-user system in practice, not a real multi-tenant gap, the same discipline used for several earlier phases' own scope notes.

**"Important unanswered email" (the sixth PROMPT.md monitor example) is deliberately not implemented** — `MonitorType` has exactly five values, not six, matching the standing Phase 20-21 (Gmail) deferral already in DECISION_LOG.md. Re-adding it is a small, additive change once Gmail integration is revisited; it is not scaffolded ahead of time (No Future Scaffolding Rule).

**The Phase 1 throwaway job pipeline is replaced with a real, typed one.** `apps/scheduler/main.py`/`apps/worker/main.py` had carried a placeholder `echo:jobs:test` string-payload job since Phase 1, with their own docstrings already naming this phase as the moment it would be replaced. It now is: `application/orchestrators/monitoring.py`'s `MonitoringEvaluateInput` (an empty Pydantic model — the sweep needs no input, only a trigger) is wrapped in `core/jobs/envelope.py`'s real `JobEnvelope[InputT]`, enqueued every 300 seconds (matching Calendar's own 5-minute cache-TTL precedent from Phase 10) to a dedicated `echo:jobs:monitoring` Redis list, and consumed by the worker, which builds all four real Postgres-backed services plus real provider factories per job and calls `evaluate_all_enabled_monitors()` inside a real `session_scope()`. Malformed JSON and per-job exceptions are caught and logged without crashing the consume loop.

**A real Docker gotcha surfaced during live verification: the worker/scheduler container images were stale.** `docker cp`-ing the new code into the running `echo-worker-1`/`echo-scheduler-1` containers and restarting them failed with `ModuleNotFoundError: No module named 'cryptography'` — those two images had never been rebuilt since before Phase 10/12 added the `cryptography` dependency for OAuth-token encryption at rest, even though `docker-compose.yml` builds all three (`backend`/`worker`/`scheduler`) from the same `./echo` context. `docker compose build worker scheduler && docker compose up -d worker scheduler` (a real image rebuild, not just a file copy) resolved it; the backend was also rebuilt afterward for the same durability reason (a `docker cp` overlay is lost if the container is ever recreated without a matching rebuild). Worth remembering for any future phase that adds a new backend dependency: the worker/scheduler images need rebuilding too, not just the backend's.

**Verified live** against the real backend/worker/scheduler/Redis, not fixtures: created a real `integration_failure` monitor via `POST /monitors` for a throwaway `live_verify_user`, triggered `POST /monitors/evaluate` and confirmed a real `EvaluationRun` with `triggered=false` / `"no integration failures recorded"` (honest, since no real failure existed for that user at verification time), and confirmed via `GET /monitors?user_id=` that the monitor persisted correctly. Independently confirmed the real scheduled pipeline end-to-end by watching container logs after rebuild: `echo-scheduler-1` logged `"enqueued monitoring.evaluate job job_..."` and `echo-worker-1` logged `"monitoring.evaluate: 1 monitor(s) evaluated"` within the same second, proving the real `JobEnvelope`/Redis/worker path (not the API's on-demand path) drives the monitor sweep.

Final state: `ruff format --check`, `ruff check`, `mypy` (0 issues across 189 source files), `check_architecture.py`, `check_size_limits.py`, `vulture`, `bandit`, `pip-audit` all clean on the backend. 544/544 backend tests passing (456 unit + 9 architecture + 79 integration). Migration `77dfbc4c290e` (3 new tables: `system_monitor_definitions`, `system_alerts`, `system_evaluation_runs`) applied to real Neon and confirmed reversible (`downgrade -1` / `upgrade head`). No new dependencies. No frontend changes this phase (Phase 24 has no dashboard-visible surface of its own yet — monitors/alerts are API-only until a future phase's UI need).
